# 检查API网关证书配置步骤

## 🎯 目标
确认API网关是否已绑定 `api.ukliving.cn` 域名，以及是否使用了正确的SSL证书。

---

## 📋 详细检查步骤

### 步骤1：进入API网关控制台

1. **访问API网关**
   - 链接：https://console.cloud.tencent.com/apigateway
   - 登录你的腾讯云账号

2. **定位你的API网关服务**
   - 在服务列表中找到你的API网关服务
   - 服务名称可能是：
     - `SCF_API_SERVICE`
     - `service-xxx-xxx`
     - 或包含你的云函数名称的服务

3. **进入服务详情**
   - 点击服务名称进入详情页面

---

### 步骤2：查看自定义域名配置

1. **点击"自定义域名"菜单**
   - 在左侧菜单或顶部标签中找到"自定义域名"
   - 点击进入

2. **查看域名列表**

#### 情况A：列表中有 `api.ukliving.cn`

**说明**：域名已绑定 ✅

**需要查看的信息**：
- **域名**：应该是 `api.ukliving.cn`
- **协议**：应该是 `HTTPS` 或 `HTTP与HTTPS`
- **证书**：查看使用的是哪个证书
- **状态**：应该是"已绑定"或"运行中"

**如何查看证书详情**：
- 点击域名进入详情，或点击"编辑"
- 查看"SSL证书"字段
- 应该显示证书名称或ID

**验证证书是否正确**：
- ✅ 正确：应该使用绑定域名为 `api.ukliving.cn` 的证书
- ❌ 错误：如果使用的是 `ukliving.cn` 的证书，需要修改

#### 情况B：列表为空或没有 `api.ukliving.cn`

**说明**：域名还未绑定 ❌

**需要做的事**：
- 添加自定义域名绑定
- 继续执行步骤3

---

### 步骤3：如果域名未绑定，添加域名绑定

1. **点击"新建"或"绑定自定义域名"**
   - 在自定义域名页面点击"新建"按钮

2. **填写域名配置**

**基本信息**：
```
域名：api.ukliving.cn
协议：HTTPS（推荐）或 HTTP与HTTPS
网络类型：公网
```

**SSL证书**：
```
⚠️ 重要：必须选择绑定域名为 api.ukliving.cn 的证书
```

**如何选择正确的证书**：
- 在证书下拉列表中，应该能看到两个证书
- **选择绑定域名为 `api.ukliving.cn` 的证书**
- **不要选择 `ukliving.cn` 的证书**（域名不匹配）

**路径映射**：
```
默认路径：/
环境：发布环境
后端地址：（应该自动关联到你的云函数）
```

3. **提交保存**
   - 点击"提交"或"确定"
   - 等待配置生效（通常1-5分钟）

---

### 步骤4：验证配置

#### 4.1 在浏览器测试

1. **打开浏览器**
2. **访问健康检查接口**：
   ```
   https://api.ukliving.cn/health
   ```

**预期结果**：
```json
{
  "status": "healthy",
  "message": "Property API is running"
}
```

**检查SSL证书**：
- 点击浏览器地址栏的锁图标
- 查看证书信息
- 确认证书域名是 `api.ukliving.cn`
- 确认证书有效且未过期

#### 4.2 测试API接口

在浏览器访问：
```
https://api.ukliving.cn/api/properties?page=1&limit=10
```

**预期结果**：
- 返回房产列表JSON数据
- 没有SSL错误
- 响应正常

---

## 🔍 证书选择说明

### 你有两个证书：

#### 证书1：api.ukliving.cn
- **绑定域名**：`api.ukliving.cn`
- **适用于**：`api.ukliving.cn`
- **是否应该使用**：✅ **是**（当前应该用这个）

#### 证书2：ukliving.cn、www.ukliving.cn
- **绑定域名**：`ukliving.cn`、`www.ukliving.cn`
- **适用于**：`ukliving.cn` 和 `www.ukliving.cn`
- **是否应该使用**：❌ **否**（不能用于 api.ukliving.cn）

### 为什么不能混用？

SSL证书的域名必须**完全匹配**访问的域名：
- 访问 `api.ukliving.cn` → 必须使用证书1
- 访问 `ukliving.cn` → 必须使用证书2
- 访问 `www.ukliving.cn` → 可以使用证书2

如果域名不匹配，浏览器会显示SSL错误。

---

## 📸 参考截图说明

### 自定义域名列表应该显示：

```
域名                  协议      证书              状态
api.ukliving.cn      HTTPS     证书-api...      已绑定
```

### 域名详情/编辑页面应该显示：

```
域名：api.ukliving.cn
协议：HTTPS ✓
SSL证书：[选择] 绑定域名：api.ukliving.cn (证书ID: xxx)
状态：已绑定
```

---

## ✅ 检查清单

完成以下检查并告诉我结果：

### API网关配置
- [ ] 已进入API网关控制台
- [ ] 找到了API网关服务
- [ ] 查看了"自定义域名"配置

### 域名绑定状态
- [ ] `api.ukliving.cn` 已绑定
- [ ] `api.ukliving.cn` 未绑定（需要添加）

### 证书配置（如果已绑定）
- [ ] 使用的是 `api.ukliving.cn` 证书 ✅ 正确
- [ ] 使用的是 `ukliving.cn` 证书 ❌ 错误，需要修改
- [ ] 不确定使用哪个证书

### 访问测试（如果已绑定）
- [ ] 可以访问 `https://api.ukliving.cn/health`
- [ ] 返回正常JSON数据
- [ ] 浏览器显示绿色锁（SSL证书有效）

---

## 🆘 常见问题

### Q1: 证书列表中看不到证书名称，只有ID？
**答**：可以通过以下方式确认：
1. 记住证书ID（如：xxxxx）
2. 到SSL证书控制台：https://console.cloud.tencent.com/ssl
3. 找到对应ID的证书
4. 查看其绑定域名

### Q2: 不小心选错证书怎么办？
**答**：可以修改：
1. 在自定义域名列表中点击"编辑"
2. 重新选择正确的SSL证书
3. 保存后等待生效

### Q3: 绑定域名后多久生效？
**答**：通常1-5分钟，最长可能10分钟。

### Q4: 配置后还是无法访问？
**答**：检查以下几点：
1. DNS解析是否正确（应该已正确）
2. SSL证书是否选择正确
3. 云函数是否正常运行
4. API网关路径映射是否正确
5. 等待5-10分钟让配置完全生效

---

## 📞 下一步

请按照上述步骤检查，然后告诉我：

1. **API网关是否已绑定 `api.ukliving.cn`？**
   - [ ] 是（已绑定）
   - [ ] 否（未绑定）

2. **如果已绑定，使用的是哪个证书？**
   - [ ] `api.ukliving.cn` 证书（正确）
   - [ ] `ukliving.cn` 证书（错误）
   - [ ] 不确定

3. **访问 `https://api.ukliving.cn/health` 是否成功？**
   - [ ] 成功（返回JSON数据）
   - [ ] 失败（SSL错误）
   - [ ] 失败（其他错误）

有了这些信息，我就能帮你完成最后的配置！
