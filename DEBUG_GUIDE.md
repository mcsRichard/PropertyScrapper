# 🚀 微信小程序 + 后端API 调试指南

## 📋 完整启动流程

### 步骤1: 启动后端API服务

在 **项目根目录** 打开命令行（PowerShell 或 CMD），运行：

```bash
python simple_api.py
```

**预期输出**：
```
[INFO] Starting Property API server on 0.0.0.0:5000
* Running on http://0.0.0.0:5000
```

✅ 看到这个说明后端服务已经启动成功！

**验证方法**：
在浏览器打开 `http://localhost:5000/health`
应该看到：`{"status": "healthy"}`

---

### 步骤2: 配置微信开发者工具

1. **打开微信开发者工具**
   - 点击"导入项目"
   - 选择项目目录：`D:\Projects\PropertyScrapper\PropertyScrapper\miniprogram`
   - 填写 AppID（可以使用测试号）

2. **配置网络请求**
   - 点击右上角 **"详情"** 按钮
   - 找到 **"本地设置"** 标签
   - ✅ 勾选 **"不校验合法域名、TLS 版本以及 HTTPS"**
   
   ⚠️ **这一步很重要！** 否则小程序无法连接本地API

3. **开始调试**
   - 点击"编译"按钮
   - 等待编译完成

---

### 步骤3: 验证连接

在小程序的 **控制台**（Console）中，应该能看到：

```
API连接成功: {status: "healthy"}
```

如果看到这个，说明小程序已经成功连接到后端API了！🎉

---

## 🛠️ 常见问题

### 问题1: API连接失败
**错误**：`Failed to connect to API`

**解决方案**：
1. 确认 `simple_api.py` 正在运行
2. 检查是否勾选了"不校验合法域名"
3. 确认 `app.js` 中的 API 地址是 `http://localhost:5000`

### 问题2: 数据加载失败
**错误**：`网络错误` 或 `加载失败`

**解决方案**：
1. 检查后端是否返回了数据：
   ```bash
   curl http://localhost:5000/api/properties
   ```
2. 在小程序控制台查看具体错误信息
3. 确认数据库中有数据（已导入28条房产记录）

### 问题3: 端口被占用
**错误**：`Address already in use`

**解决方案**：
1. 关闭占用5000端口的程序
2. 或者修改 `simple_api.py` 中的端口号：
   ```python
   app.run(host='0.0.0.0', port=5001)  # 改为5001
   ```
3. 同时修改 `app.js` 中的 API 地址

---

## 📊 调试技巧

### 1. 查看网络请求
在微信开发者工具的 **Network** 面板中可以看到所有API请求

### 2. 查看日志
在小程序控制台中查看：
- `API连接成功` - 后端服务正常
- `加载中...` - 正在请求数据
- `加载失败` - API请求失败

### 3. 测试单个接口
在浏览器中直接访问：
- `http://localhost:5000/api/properties` - 房产列表
- `http://localhost:5000/api/properties/1` - 房产详情

---

## 🎯 当前状态

✅ **已完成**：
- 后端API服务运行在 `localhost:5000`
- 数据库中有28条房产数据
- 小程序项目结构完整
- API接口正常工作

⏳ **进行中**：
- 微信开发者工具调试配置
- 网络请求配置
- 功能测试

---

## 📝 下一步

完成调试后，您可以：
1. 测试房产列表展示
2. 测试房产详情页面
3. 测试分页加载功能
4. 根据需要调整UI和功能

**需要帮助？** 查看上面的常见问题或告诉我具体的错误信息！

