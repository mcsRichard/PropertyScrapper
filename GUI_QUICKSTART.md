# GUI 快速启动指南

## 快速开始（3步）

### 1️⃣ 安装依赖

```bash
pip install Pillow requests
```

### 2️⃣ 启动GUI

```bash
python run_gui.py
```

或者直接运行：

```bash
python property_gui.py
```

### 3️⃣ 使用GUI

1. 启动后会自动加载 `properties.csv`
2. 点击"下载所有图片"下载房产图片
3. 在列表中点击房产查看详情

---

## 功能演示

### 图片存储功能

✅ **自动下载**: 点击"下载所有图片"自动保存所有房产图片  
✅ **本地存储**: 图片保存在 `property_images/` 目录  
✅ **智能缓存**: 已下载的图片不会重复下载  
✅ **断点续传**: 支持中断后继续下载  

### GUI显示功能

✅ **列表浏览**: 左侧显示所有房产列表  
✅ **图片预览**: 右上角显示房产图片  
✅ **详情查看**: 右下角显示完整房产信息  
✅ **搜索过滤**: 实时搜索房产  
✅ **导航功能**: 支持上一个/下一个导航  

---

## 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  工具栏: [加载CSV] [下载所有图片] [清除图片缓存]      │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  搜索: ___  │                                           │
│             │        房产图片显示区域                    │
│  ┌────────┐ │                                           │
│  │ 房产1  │ │                                           │
│  │ 房产2  │ │                                           │
│  │ 房产3  │ │                                           │
│  │   ...  │ │                                           │
│  │        │ │                                           │
│  └────────┘ └───────────────────────────────────────────┤
│             │                                           │
│             │  房产详细信息:                              │
│             │  ┌────────────────────────────────────┐    │
│             │  │ 标题: 2 bed flat for sale         │    │
│             │  │ 价格: £575000                     │    │
│             │  │ 描述: 明亮宽敞的两卧室...          │    │
│             │  └────────────────────────────────────┘    │
├─────────────┴───────────────────────────────────────────┤
│  [上一个] [下一个]          第 3 / 28                     │
└─────────────────────────────────────────────────────────┘
```

---

## 使用示例

### 示例 1: 浏览房产

1. 启动GUI后，房产列表自动加载
2. 点击左侧列表中的任意房产
3. 右侧显示该房产的图片和详细信息

### 示例 2: 下载图片

1. 点击"下载所有图片"按钮
2. 确认下载（将下载28张图片）
3. 等待下载完成
4. 完成后图片自动在GUI中显示

### 示例 3: 搜索房产

1. 在搜索框输入关键词（如"flat"）
2. 列表实时过滤
3. 点击结果查看详情

### 示例 4: 清除缓存

1. 点击"清除图片缓存"
2. 确认删除
3. 所有下载的图片将被删除

---

## 常见问题

### ❓ 图片显示不出来？

**解决方案**:
1. 点击"下载所有图片"按钮
2. 等待下载完成
3. 图片会自动显示

### ❓ 下载速度很慢？

**原因**: 需要从Zoopla服务器下载图片  
**解决方案**: 耐心等待，或使用VPN加速

### ❓ 如何导出数据？

**当前版本**: GUI仅用于浏览和查看  
**未来版本**: 计划添加导出功能

### ❓ 支持哪些操作系统？

**支持**: Windows, macOS, Linux  
**注意**: 需要Python 3.6+和tkinter支持

---

## 高级功能

### 自定义图片目录

编辑 `property_gui.py`:

```python
# 第28行
self.image_dir = "custom_images"  # 修改为你的目录
```

### 修改图片大小

编辑 `property_gui.py`:

```python
# 第253行
img = self.resize_image(img, max_size=(800, 600))  # 修改尺寸
```

### 添加导出功能

在GUI中添加导出按钮:

```python
tk.Button(toolbar, text="导出为JSON", command=self.export_json).pack(side=tk.LEFT)
```

---

## 系统要求

- Python 3.6 或更高版本
- tkinter (通常随Python安装)
- Pillow 10.0+
- requests 2.28+

### Windows

```bash
# tkinter通常已包含
pip install Pillow requests
```

### macOS

```bash
pip3 install Pillow requests
```

### Ubuntu/Debian

```bash
sudo apt-get install python3-tk
pip3 install Pillow requests
```

---

## 性能优化

### 减少内存使用

编辑 `property_gui.py` 第34行:

```python
# 清理旧图片缓存
if len(self.loaded_images) > 50:
    self.loaded_images.clear()
```

### 批量下载延迟

编辑下载函数，添加延迟:

```python
# 在第287行后添加
time.sleep(0.5)  # 延迟0.5秒
```

---

## 下一步计划

- [ ] 添加导出为Excel功能
- [ ] 添加数据统计图表
- [ ] 添加收藏功能
- [ ] 添加数据筛选
- [ ] 添加批量操作

---

## 技术支持

遇到问题？请提供：
1. Python版本: `python --version`
2. 错误信息: 完整的错误堆栈
3. 操作系统: Windows/macOS/Linux
4. 执行步骤: 描述你做了什么

祝使用愉快！🎉

