# 域名配置问题排查

## 📊 当前状态检测结果

### ✅ DNS解析 - 正常
- **域名**：`api.ukliving.cn`
- **记录类型**：CNAME
- **记录值**：`1383789184-lebyqyy34m.ap-shanghai.tencentscf.com`
- **解析IP**：175.24.161.166; 124.223.145.105; 124.223.147.76
- **状态**：✅ 解析成功，TTL 600秒

### ❌ HTTPS访问 - 失败
- **错误信息**：`Could not create SSL/TLS secure channel`
- **问题原因**：SSL/TLS证书配置有问题

---

## 🔍 问题诊断

### 可能的原因

#### 1. API网关未绑定自定义域名
虽然DNS解析正确，但API网关可能还没有绑定 `api.ukliving.cn` 这个域名。

#### 2. SSL证书未绑定或配置错误
- SSL证书可能还没有绑定到API网关
- SSL证书的域名可能不匹配（证书是 `ukliving.cn`，但访问的是 `api.ukliving.cn`）

#### 3. SSL证书域名不匹配
如果SSL证书是为 `ukliving.cn` 申请的，但访问的是 `api.ukliving.cn`，会导致证书验证失败。

---

## 🔧 解决步骤

### 步骤1：检查API网关域名绑定

1. **登录腾讯云API网关控制台**
   - 访问：https://console.cloud.tencent.com/apigateway

2. **找到你的API网关服务**
   - 服务名称应该类似：`SCF_API_SERVICE` 或包含你的云函数名称

3. **检查自定义域名**
   - 点击服务名称进入详情
   - 点击左侧菜单"自定义域名"
   - 查看是否有 `api.ukliving.cn` 的绑定记录

**预期结果**：
- ✅ 应该看到 `api.ukliving.cn` 已绑定
- ✅ 协议为 HTTPS
- ✅ SSL证书已选择

**如果没有绑定**：
- 需要点击"新建"添加域名绑定
- 继续执行步骤2

---

### 步骤2：检查SSL证书

1. **查看SSL证书详情**
   - 访问：https://console.cloud.tencent.com/ssl
   - 点击"我的证书"
   - 查看已签发的证书

2. **检查证书域名**
   - 查看证书的"绑定域名"字段
   - 确认是否包含 `api.ukliving.cn`

**关键问题**：证书必须覆盖你要使用的域名

#### 情况A：证书域名是 `ukliving.cn`（不包含 api 子域名）
- ❌ 这个证书**不能**用于 `api.ukliving.cn`
- 需要重新申请证书，或者改用主域名 `ukliving.cn`

#### 情况B：证书域名是 `api.ukliving.cn`
- ✅ 这个证书可以用于 `api.ukliving.cn`
- 继续检查是否已绑定到API网关

#### 情况C：证书域名是 `*.ukliving.cn`（泛域名）
- ✅ 这个证书可以用于 `api.ukliving.cn`、`www.ukliving.cn` 等所有子域名
- 继续检查是否已绑定到API网关

---

### 步骤3：绑定域名到API网关（如果还未绑定）

1. **进入API网关控制台**
   - https://console.cloud.tencent.com/apigateway

2. **进入你的API网关服务**
   - 点击服务名称进入详情

3. **添加自定义域名**
   - 点击左侧"自定义域名"
   - 点击"新建"

4. **填写配置**
   - **域名**：`api.ukliving.cn`
   - **协议**：选择 `HTTPS` 或 `HTTP与HTTPS`（推荐HTTPS）
   - **网络类型**：`公网`
   - **SSL证书**：选择对应的证书
     - ⚠️ 如果证书列表为空或没有匹配的证书，需要先申请证书
   - **路径映射**：
     - 默认路径：`/`
     - 环境：`发布环境`
     - 后端地址：自动关联云函数

5. **提交保存**
   - 点击"提交"
   - 等待配置生效（通常几分钟）

---

### 步骤4：如果证书域名不匹配，需要重新申请

**如果你的SSL证书是 `ukliving.cn`，而你要使用 `api.ukliving.cn`**：

#### 选项A：为 `api.ukliving.cn` 申请新证书

1. 访问：https://console.cloud.tencent.com/ssl
2. 点击"申请免费证书"
3. 填写域名：`api.ukliving.cn`
4. 完成DNS验证（添加TXT记录）
5. 等待证书签发（0.5-2小时）
6. 将新证书绑定到API网关

#### 选项B：改用主域名 `ukliving.cn`

如果你的SSL证书已经是 `ukliving.cn`，可以：

1. **修改DNS解析**：
   - 删除或暂停 `api` 的CNAME记录
   - 添加 `@` 的CNAME记录（主域名）
   - 记录值：`1383789184-lebyqyy34m.ap-shanghai.tencentscf.com`

2. **在API网关绑定主域名**：
   - 绑定域名改为：`ukliving.cn`
   - 使用现有的SSL证书

3. **修改小程序代码**：
   - 将 `apiBaseUrl` 改为 `https://ukliving.cn`
   - （我已经按 `api.ukliving.cn` 修改了代码，需要再次调整）

#### 选项C：申请泛域名证书（推荐，但可能需要付费）

申请 `*.ukliving.cn` 证书，可以同时支持：
- `ukliving.cn`（主域名）
- `api.ukliving.cn`
- `www.ukliving.cn`
- 其他任何子域名

**注意**：泛域名证书可能需要付费，具体看腾讯云的政策。

---

## 🎯 推荐方案

### 方案1：使用子域名 `api.ukliving.cn`（当前配置）

**优点**：
- 结构清晰，API和网站分离
- 便于管理和扩展

**需要做的事**：
1. ✅ DNS解析已配置（CNAME记录已添加）
2. ❓ 检查SSL证书是否覆盖 `api.ukliving.cn`
3. ❓ 在API网关绑定 `api.ukliving.cn`
4. ✅ 小程序代码已更新为 `https://api.ukliving.cn`

### 方案2：使用主域名 `ukliving.cn`

**优点**：
- 域名更简洁
- 如果SSL证书已经是主域名，直接可用

**需要做的事**：
1. 修改DNS解析（将 `@` 指向API网关CNAME）
2. 在API网关绑定 `ukliving.cn`
3. 修改小程序代码为 `https://ukliving.cn`

---

## ✅ 下一步操作清单

### 立即检查（按顺序）

1. **检查SSL证书域名**
   - [ ] 登录：https://console.cloud.tencent.com/ssl
   - [ ] 查看证书的"绑定域名"
   - [ ] 确认证书是否覆盖 `api.ukliving.cn`

2. **检查API网关域名绑定**
   - [ ] 登录：https://console.cloud.tencent.com/apigateway
   - [ ] 查看"自定义域名"
   - [ ] 确认是否已绑定 `api.ukliving.cn`

3. **根据检查结果采取行动**：

   **如果证书域名是 `api.ukliving.cn` 且已绑定到API网关**：
   - ✅ 配置完成，应该可以访问
   - 等待几分钟后重试

   **如果证书域名是 `api.ukliving.cn` 但未绑定到API网关**：
   - 在API网关中绑定域名和证书
   - 等待生效后测试

   **如果证书域名是 `ukliving.cn`（不包含api）**：
   - **选项A**：为 `api.ukliving.cn` 重新申请证书
   - **选项B**：改用主域名 `ukliving.cn`

---

## 📝 需要的信息

请检查并告诉我：

1. **SSL证书的域名是什么？**
   - 在腾讯云SSL证书控制台查看
   - 截图或告知证书的"绑定域名"

2. **API网关是否已绑定 `api.ukliving.cn`？**
   - 在API网关控制台的"自定义域名"中查看
   - 截图或告知绑定状态

有了这两个信息，我就能准确判断问题并给出解决方案！

---

## 🔗 相关链接

- **腾讯云SSL证书**：https://console.cloud.tencent.com/ssl
- **腾讯云API网关**：https://console.cloud.tencent.com/apigateway
- **DNS管理**：https://console.cloud.tencent.com/cns
