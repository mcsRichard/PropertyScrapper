# 快速开始 - 配置腾讯云COS

## ⚡ 3步完成配置

### 第1步：获取API密钥（1分钟）

**最简单方法**：直接访问
```
https://console.cloud.tencent.com/cam/capi
```

登录后：
1. 点击 **新建密钥**（如果没有密钥）
2. 复制 `SecretId` 和 `SecretKey`

---

### 第2步：创建COS存储桶（2分钟）

访问：
```
https://console.cloud.tencent.com/cos
```

点击 **创建存储桶**，填写：
- **名称**: `property-images`
- **地域**: 选择离你最近的（如上海）
- **访问权限**: `公有读私有写`
- **存储类型**: `标准存储`

点击 **确定** 创建

---

### 第3步：配置项目（1分钟）

在 `backend` 目录创建 `.env` 文件：

```env
# 腾讯云COS配置
COS_SECRET_ID=AKIDxxxxx      # 从第1步获取
COS_SECRET_KEY=xxxxx         # 从第1步获取
COS_REGION=ap-shanghai       # 存储桶地域
COS_BUCKET=property-images   # 存储桶名称
COS_DOMAIN=                  # 留空（如果不用自定义CDN域名）
```

**⚠️ 注意**：
- 将 `AKIDxxxxx` 替换为你的实际 SecretId
- 将 `xxxxx` 替换为你的实际 SecretKey
- 根据你的存储桶地域修改 `COS_REGION`

---

## ✅ 验证配置

安装依赖：

```bash
pip install cos-python-sdk-v5
```

运行爬虫：

```bash
python Scrapper.py
```

如果看到以下输出，说明配置成功：

```
[COS] 正在下载: https://lid.zoocdn.com/...
[COS] 正在上传到: property-images/2024/11/15/xxx.jpg
[COS] 上传成功: https://property-images.cos.ap-shanghai.myqcloud.com/...
```

---

## 🎉 完成！

现在爬虫会自动：
1. ✅ 爬取房产数据
2. ✅ 下载图片
3. ✅ 上传到腾讯云COS
4. ✅ 将COS链接保存到CSV

微信小程序将自动显示COS上的图片！

---

## 📚 更多帮助

- 详细步骤：查看 `COS_SETUP.md`
- 密钥问题：查看 `获取腾讯云API密钥详细步骤.md`
- 完整说明：查看 `图片上传到COS说明.md`

## 🆘 遇到问题？

1. **找不到"访问管理"**？
   → 使用直接链接：https://console.cloud.tencent.com/cam/capi

2. **上传失败**？
   → 检查 `.env` 文件中的配置是否正确
   → 确认存储桶权限设置为"公有读私有写"

3. **小程序图片不显示**？
   → 检查图片URL是否正确
   → 确认存储桶已设置为"公有读"

需要帮助？查看详细文档！

