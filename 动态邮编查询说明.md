# 动态邮编查询功能说明

## 功能概述

系统已实现动态邮编查询功能，支持全英国任何地区的邮编查询，无需维护静态字典。使用Nominatim API（OpenStreetMap的地理编码服务）进行实时查询。

## 主要特性

### 1. 动态API查询
- ✅ **使用Nominatim API**：免费、开放的地理编码服务
- ✅ **国内可访问**：无需翻墙，直接访问
- ✅ **覆盖全英国**：支持任何英国地名查询邮编
- ✅ **自动降级**：API失败时自动使用静态字典或原始location

### 2. 智能缓存机制
- ✅ **内存缓存**：同一地名在内存中只查询一次
- ✅ **请求限流**：自动控制请求频率，避免触发API限流（1秒/次）
- ✅ **错误缓存**：失败的查询也会缓存，避免重复失败请求

### 3. 完全动态查询
- ✅ **纯API查询**：所有地名都通过Nominatim API动态查询，无需维护静态字典
- ✅ **智能缓存**：查询结果缓存到内存，避免重复请求
- ✅ **原始location备用**：如果API失败，使用原始location进行模糊搜索

## 工作流程

```
用户输入地名（如"利兹大学"或"N10"）
    ↓
1. 检查是否已是邮编格式 → 是则直接使用
    ↓
2. 检查内存缓存 → 找到则返回缓存结果
    ↓
3. 调用Nominatim API → 动态查询邮编
    ↓
4. 提取邮编前缀 → 存入缓存
    ↓
5. 返回邮编前缀列表 → 用于数据库搜索
    ↓
6. 如果API失败 → 使用原始location进行模糊搜索
```

## 技术实现

### Nominatim API
- **API地址**: `https://nominatim.openstreetmap.org/search`
- **免费使用**: 无需注册，无需API密钥
- **请求限制**: 建议1秒/次（已自动控制）
- **返回格式**: JSON格式的地理编码结果
- **国内访问**: ✅ 可正常访问，无需翻墙

### 查询示例

**输入**: "Leeds University"
**API查询**: `https://nominatim.openstreetmap.org/search?q=Leeds University, UK&format=json&countrycodes=gb&limit=5`
**返回结果**: 
```json
[
  {
    "address": {
      "postcode": "LS2 9JT"
    }
  }
]
```
**提取邮编前缀**: `LS2`

## 配置说明

### 环境变量（可选）
可以在配置文件中调整以下参数（默认值通常足够）：

```python
# 在 location_mapper.py 中可以调整：
REQUEST_DELAY = 1.0  # 请求延迟（秒）
NOMINATIM_TIMEOUT = 5  # 请求超时时间（秒）
```

### 禁用API查询
如果不想使用API查询（仅使用静态字典），可以在代码中设置：

```python
LocationMapper.location_to_search_term(location, use_api=False)
```

## 使用示例

### 示例1：任何地名（API查询）
```
输入: "帝国理工大学"
API查询: 调用Nominatim
返回: "SW7,SW3"（从API获取，并缓存）
```

### 示例2：非常见地名（API查询）
```
输入: "利兹大学"
API查询: 调用Nominatim
返回: "LS2,LS1"（从API获取，并缓存）
```

### 示例3：邮编格式（直接使用）
```
输入: "N10"
检测: 邮编格式
返回: "N10"（直接使用，不调用API）
```

### 示例4：已输入邮编
```
输入: "SW7"
输出: 直接识别为邮编，不查询API
```

## 调试信息

系统会输出详细的调试信息，帮助排查问题：

```
[LOCATION-MAPPER] 使用Nominatim API查询: 利兹大学
[LOCATION-MAPPER] ✅ 成功查询到邮编: 利兹大学 -> ['LS2', 'LS1']
[AI-SEARCH] 地名映射成功: 利兹大学 -> LS2,LS1
```

## 注意事项

### 1. API限流
Nominatim API有使用限制：
- **免费用户**: 建议1秒/次（已自动控制）
- **高频率使用**: 建议自建Nominatim实例或使用其他付费服务

### 2. 查询准确性
- 地名需要明确，建议包含"University"、"College"等关键词
- 对于模糊地名，API可能返回多个结果，系统会提取所有邮编前缀

### 3. 网络依赖
- API查询需要网络连接
- 如果网络不可用，会自动降级到静态字典或原始location

### 4. 缓存策略
- 内存缓存：程序运行期间有效
- 程序重启后缓存清空（可考虑实现持久化缓存）

## 性能优化

### 当前优化措施
1. ✅ **静态字典优先**：常见地名无需API查询
2. ✅ **内存缓存**：同一地名只查询一次
3. ✅ **请求限流**：避免触发API限制
4. ✅ **错误缓存**：失败的查询也会缓存

### 未来优化方向
1. 持久化缓存（文件/数据库）
2. 批量查询优化
3. 本地Nominatim实例部署

## 优势

| 特性 | 说明 |
|------|------|
| 覆盖范围 | 全英国任何地区，无需维护 |
| 实时性 | API自动更新，数据准确 |
| 维护成本 | 零维护，无需手动添加地名 |
| 扩展性 | 支持任何新地名，自动适应 |
| 智能缓存 | 内存缓存减少重复请求 |
| 容错机制 | API失败时降级到原始location模糊搜索 |

**结论**: 完全使用API查询，实现了真正的动态化，支持全英国任何地名，无需手动维护。

## 故障排查

### 问题1：API查询失败
**现象**: 日志显示 `❌ 请求异常`
**解决**: 
- 检查网络连接
- 检查是否被API限流
- 系统会自动降级到静态字典或原始location

### 问题2：查询结果不准确
**现象**: 返回的邮编不正确
**解决**: 
- 检查输入地名是否准确
- 可以手动在静态字典中添加更精确的映射

### 问题3：查询速度慢
**现象**: 每次查询都调用API
**解决**: 
- 检查内存缓存是否正常工作
- 考虑添加更多静态字典条目

## 相关文件

- `backend/utils/location_mapper.py` - 邮编映射核心实现
- `backend/api/properties.py` - API端点，调用邮编映射
- `backend/utils/database.py` - 数据库查询，支持邮编前缀搜索

## 参考资料

- [Nominatim API文档](https://nominatim.org/release-docs/latest/api/Search/)
- [OpenStreetMap](https://www.openstreetmap.org/)
- [英国邮编格式说明](https://en.wikipedia.org/wiki/Postcodes_in_the_United_Kingdom)

