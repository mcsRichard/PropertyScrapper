# 云函数自定义域名配置指南（API网关替代方案）

## ⚠️ 重要变更说明

**API网关已停服**（2025年6月30日）：
- 原来通过API网关访问云函数的方式已不可用
- 需要使用**云函数自定义域名**或**函数URL**功能替代

---

## 🎯 解决方案：云函数自定义域名

腾讯云函数（SCF）提供了**自定义域名**功能，可以直接绑定域名到云函数，无需API网关。

---

## 📋 配置步骤

### 步骤1：进入云函数控制台

1. **访问云函数控制台**
   - 链接：https://console.cloud.tencent.com/scf
   - 登录你的腾讯云账号

2. **找到你的云函数**
   - 在函数列表中找到你部署的函数
   - 函数名称可能包含：`property-api` 或类似名称
   - 点击函数名称进入详情

---

### 步骤2：查看当前访问地址

1. **查看触发管理**
   - 点击"触发管理"标签
   - 查看现有的触发器配置

2. **可能的情况**：

#### 情况A：有HTTP触发器或Web函数
- 会显示一个默认访问URL
- 格式：`https://[function-id]-[region].scf.tencentcs.com/`
- 这个URL可以直接使用（但是是临时域名）

#### 情况B：只有API网关触发器（已失效）
- 显示API网关触发器，但已不可用
- 需要创建新的HTTP触发器或配置自定义域名

---

### 步骤3：配置自定义域名

#### 3.1 进入自定义域名配置

1. **在云函数详情页面**
   - 找到"触发管理"或"触发器配置"
   - 或者在左侧菜单找到"自定义域名"

2. **点击"新建自定义域名"**
   - 如果看不到这个选项，说明需要先创建HTTP触发器

#### 3.2 创建HTTP触发器（如果需要）

如果还没有HTTP触发器：

1. **点击"创建触发器"**
2. **选择触发方式**：
   - 选择 `HTTP触发器` 或 `Web函数触发器`
3. **配置触发器**：
   ```
   触发方式：HTTP触发器
   路径：/ （或留空）
   方法：ANY（支持所有HTTP方法）
   启用集成响应：是
   ```
4. **保存配置**
   - 保存后会生成一个默认访问URL
   - 记录这个URL，可以先用它测试

#### 3.3 绑定自定义域名

1. **找到"自定义域名"配置**
   - 在云函数控制台左侧菜单
   - 或在函数详情页的"触发管理"中

2. **点击"新建自定义域名"**

3. **填写域名配置**：

**基本配置**：
```
自定义域名：api.ukliving.cn
协议：HTTPS（推荐）或 HTTP & HTTPS
网络类型：公网
```

**SSL证书**：
```
选择证书：选择绑定域名为 api.ukliving.cn 的证书
```

**路径映射**：
```
路径：/
函数：选择你的云函数（property-api）
版本：$DEFAULT 或 LATEST
```

**其他配置**：
```
启用CORS：是（小程序需要）
```

4. **保存配置**
   - 点击"确定"或"提交"
   - 等待配置生效（通常1-5分钟）

---

### 步骤4：配置DNS解析（更新CNAME）

#### 4.1 获取云函数的CNAME地址

绑定自定义域名后，系统会提供一个CNAME地址，格式类似：
```
xxx-xxx-xxx.scf.tencentcs.com
```

**注意**：这个CNAME地址与之前API网关的不同！

#### 4.2 更新DNS记录

1. **访问DNS管理**
   - 链接：https://console.cloud.tencent.com/cns
   - 找到域名 `ukliving.cn`

2. **修改现有的CNAME记录**
   - 找到主机记录 `api` 的CNAME记录
   - 点击"修改"

3. **更新记录值**：
   ```
   记录类型：CNAME
   主机记录：api
   记录值：[新的云函数CNAME地址]（替换旧的API网关地址）
   TTL：600
   ```

4. **保存并等待生效**
   - DNS更新通常10分钟-2小时生效

---

## 🔍 查找云函数访问地址的方法

### 方法1：通过云函数控制台

1. 访问：https://console.cloud.tencent.com/scf/list
2. 找到你的函数（property-api）
3. 点击函数名称进入详情
4. 查看"触发管理"标签
5. 查看HTTP触发器或Web函数的访问URL

### 方法2：查看现有配置

你之前使用的临时域名：
```
https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com
```

这个域名格式表明是**上海地域的云函数URL**。

**分析**：
- `1383789184`：可能是账号ID或函数ID
- `lebyqyy34m`：函数标识符
- `ap-shanghai`：上海地域
- `tencentscf.com`：云函数域名

这个URL应该仍然可以使用（如果函数还在运行）。

---

## ✅ 验证配置

### 1. 测试云函数默认URL

先测试云函数的默认URL是否正常：

```bash
# 测试原临时域名
https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com/health
```

**如果可以访问**：
- ✅ 说明云函数正常运行
- 只需要配置自定义域名即可

**如果无法访问**：
- ❌ 可能需要重新部署云函数
- 或者创建新的HTTP触发器

### 2. 测试自定义域名

配置完成后，测试：

```bash
# 健康检查
https://api.ukliving.cn/health

# API接口
https://api.ukliving.cn/api/properties?page=1&limit=10
```

**预期结果**：
- 返回正常JSON数据
- SSL证书有效
- 浏览器显示绿色锁

---

## 📝 配置对比

### 旧方案（API网关，已停服）

```
云函数 → API网关触发器 → API网关服务 → 自定义域名
DNS: api.ukliving.cn → API网关CNAME
```

### 新方案（云函数自定义域名）

```
云函数 → HTTP触发器 → 自定义域名
DNS: api.ukliving.cn → 云函数CNAME
```

**更简单、更直接！**

---

## 🔧 如果找不到云函数或配置

### 可能的情况

#### 1. 云函数已部署，但没有HTTP触发器

**解决**：
1. 进入云函数详情
2. 创建HTTP触发器
3. 配置自定义域名

#### 2. 云函数还没有部署

**解决**：
1. 需要重新部署云函数
2. 参考：`scf_deploy` 目录下的部署文档
3. 确保使用Web函数模式

#### 3. 之前只配置了API网关，云函数实际是事件函数

**解决**：
1. 检查云函数类型（事件函数 vs Web函数）
2. 如果是事件函数，需要改为Web函数
3. 或者重新部署为Web函数

---

## 🎯 立即检查步骤

### 第1步：确认云函数状态

1. 访问：https://console.cloud.tencent.com/scf/list
2. 查看是否有已部署的云函数
3. 记录函数名称、地域

### 第2步：测试现有URL

在浏览器访问：
```
https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com/health
```

**如果可以访问**：
- ✅ 函数正常，直接配置自定义域名

**如果无法访问**：
- ❌ 需要检查云函数部署状态

### 第3步：配置自定义域名

根据上述步骤配置，主要是：
1. 确保有HTTP触发器
2. 绑定自定义域名 `api.ukliving.cn`
3. 选择正确的SSL证书（`api.ukliving.cn`）
4. 更新DNS记录的CNAME值

---

## 📞 下一步

请告诉我：

1. **访问临时URL是否成功**？
   ```
   https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com/health
   ```
   - [ ] 成功（返回JSON）
   - [ ] 失败（404或其他错误）

2. **在云函数控制台找到函数了吗**？
   - [ ] 找到了（函数名称：_______）
   - [ ] 没找到

3. **函数是否有HTTP触发器**？
   - [ ] 有
   - [ ] 没有（只有API网关触发器）
   - [ ] 不确定

有了这些信息，我会指导你完成自定义域名配置！

---

## 🔗 参考链接

- **云函数控制台**：https://console.cloud.tencent.com/scf
- **云函数自定义域名文档**：腾讯云官方文档
- **SSL证书管理**：https://console.cloud.tencent.com/ssl
- **DNS管理**：https://console.cloud.tencent.com/cns

---

## 💡 总结

**由于API网关停服，需要改用云函数自定义域名功能**：

1. ✅ 云函数仍然可以正常使用
2. ✅ 可以直接绑定自定义域名到云函数
3. ✅ 配置更简单，性能更好
4. ✅ SSL证书配置方式相同

**优点**：
- 减少一层转发（API网关），性能更好
- 配置更简单
- 成本更低（无API网关费用）
