# 域名切换指南：临时域名到正式域名

## 📋 当前状态
- **临时域名**：`1383789184-lebyqyy34m.ap-shanghai.tencentscf.com`
- **正式域名**：`ukliving.cn`
- **域名备案状态**：已完成 ✅

---

## 🎯 切换步骤概览

1. **申请SSL证书**（需要先完成，约0.5-2小时）
2. **配置DNS解析**（CNAME记录）
3. **在API网关绑定域名**
4. **更新小程序代码**
5. **配置微信小程序服务器域名**
6. **测试验证**

---

## 第一步：申请SSL证书（0.5-2小时）

### 1.1 进入SSL证书服务
1. 访问：https://console.cloud.tencent.com/ssl
2. 登录你的腾讯云账号

### 1.2 申请免费证书
1. 点击左侧"我的证书"
2. 点击"申请免费证书"
3. 填写信息：
   - **证书类型**：选择 `域名型免费版（DV）`
   - **域名**：选择你需要的方案

### 1.3 配置证书域名

#### 方案一：使用主域名（推荐）
- **域名**：`ukliving.cn`
- **绑定域名**：`ukliving.cn` 和 `www.ukliving.cn`
- **用途**：小程序和网站都用主域名

#### 方案二：使用子域名
- **域名**：`api.ukliving.cn`
- **用途**：专门用于API服务

**建议选择方案一**，使用主域名更简洁，且一个证书可以同时覆盖 `ukliving.cn` 和 `www.ukliving.cn`。

### 1.4 完成域名验证
1. 选择验证方式：
   - **DNS验证**（推荐）：添加TXT记录
   - **文件验证**：上传验证文件到网站根目录

2. **推荐使用DNS验证**：
   - 复制系统提供的TXT记录
   - 到域名DNS管理中添加TXT记录
   - 等待验证通过

### 1.5 等待证书签发
- 通常需要 **0.5-2小时**
- 可以在"我的证书"中查看状态
- 状态变为"已签发"即可使用

---

## 第二步：配置DNS解析

### 2.1 登录域名管理控制台
- 腾讯云域名：https://console.cloud.tencent.com/cns
- 或登录你的域名注册商控制台

### 2.2 获取API网关CNAME地址
1. 访问：https://console.cloud.tencent.com/apigateway
2. 找到你的API网关服务
3. 点击"基本信息"或"自定义域名"
4. 查看或记录CNAME地址（如：`service-xxx-xxx.apigw.tencentcs.com`）

**如果还没有显示CNAME地址**：
- 稍后在绑定域名时会自动生成
- 可以先配置DNS，暂时留空记录值，稍后更新

### 2.3 添加DNS解析记录

根据你选择的域名方案添加记录：

#### 方案一：使用主域名 `ukliving.cn`
添加以下记录：

**记录1（主域名）**：
- **记录类型**：`CNAME`
- **主机记录**：`@`
- **记录值**：API网关的CNAME地址（如：`service-xxx-xxx.apigw.tencentcs.com`）
- **TTL**：`600`（10分钟）

**记录2（www子域名）**：
- **记录类型**：`CNAME`
- **主机记录**：`www`
- **记录值**：API网关的CNAME地址
- **TTL**：`600`

#### 方案二：使用子域名 `api.ukliving.cn`
添加记录：
- **记录类型**：`CNAME`
- **主机记录**：`api`
- **记录值**：API网关的CNAME地址
- **TTL**：`600`

### 2.4 保存并等待生效
- DNS解析通常 **10分钟-2小时** 生效
- 可以使用 `nslookup` 检查是否生效

---

## 第三步：在API网关绑定域名

### 3.1 进入API网关控制台
1. 访问：https://console.cloud.tencent.com/apigateway
2. 找到你的API网关服务（通常是 `SCF_API_SERVICE` 或类似名称）
3. 点击服务名称进入详情

### 3.2 绑定自定义域名
1. 点击左侧菜单"自定义域名"
2. 点击"新建"
3. 填写信息：

**基本信息**：
- **域名**：`ukliving.cn`（或 `api.ukliving.cn`）
- **协议**：选择 `HTTPS` 或 `HTTP与HTTPS`（**推荐HTTPS**）
- **网络类型**：`公网`

**SSL证书**：
- 选择刚才申请的SSL证书
- 如果证书还在签发中，可以稍后再绑定

**路径映射**：
- **默认路径**：`/`
- **环境**：`发布环境`
- **后端地址**：选择你的云函数（应该已自动关联）

### 3.3 保存配置
1. 点击"提交"
2. 等待配置生效（通常几分钟）
3. 记录生成的CNAME地址

### 3.4 更新DNS解析（如果之前未配置）
如果之前DNS解析记录值为空，现在更新为API网关提供的CNAME地址。

---

## 第四步：更新小程序代码

### 4.1 更新API基础地址

编辑 `miniprogram/app.js`：

```javascript
// miniprogram/app.js
App({
  globalData: {
    // 更新为正式域名
    apiBaseUrl: 'https://ukliving.cn',  // ← 修改这里
    
    // 或者如果使用子域名
    // apiBaseUrl: 'https://api.ukliving.cn',
    
    authToken: null,
    userInfo: null,
    contactStats: null,
    customerServiceWechat: '',
    customerServiceQrUrl: '/image/qrcode_Richard_Test.png'
  },
  
  // ... 其他代码保持不变
})
```

### 4.2 检查其他可能硬编码的地方

搜索项目中是否有其他地方使用了临时域名：

```bash
# 在项目根目录执行
grep -r "1383789184-lebyqyy34m.ap-shanghai.tencentscf.com" miniprogram/
```

如果有其他文件使用了临时域名，也要一并更新。

### 4.3 保存修改
确保所有修改已保存。

---

## 第五步：配置微信小程序服务器域名

### 5.1 登录微信公众平台
1. 访问：https://mp.weixin.qq.com
2. 使用小程序管理员微信扫码登录
3. 进入你的小程序后台

### 5.2 配置服务器域名
1. 点击左侧菜单："开发" > "开发管理"
2. 找到"开发设置"区域
3. 找到"服务器域名"部分
4. 点击"修改"或"开始配置"

### 5.3 添加域名

#### request合法域名（必须）
添加以下域名：
```
https://ukliving.cn
```

或者如果使用子域名：
```
https://api.ukliving.cn
```

#### downloadFile合法域名（必须）
添加以下域名：
```
https://ukliving.cn
https://your-bucket.cos.ap-shanghai.myqcloud.com
```

**注意**：
- 第二个是COS图片域名，确保添加你的实际COS域名
- 在 `backend/config.py` 中可以找到 `COS_DOMAIN` 配置

#### uploadFile合法域名（如果需要上传功能）
```
https://ukliving.cn
```

### 5.4 保存配置
1. 点击"保存"
2. 可能需要管理员微信扫码确认

### 5.5 注意事项
- 域名必须是HTTPS
- 域名必须已备案
- 每个域名最多配置20个
- 修改后立即生效

---

## 第六步：测试验证

### 6.1 验证DNS解析

**Windows PowerShell**：
```powershell
# 检查DNS解析
Resolve-DnsName ukliving.cn -Type CNAME
```

**Mac/Linux**：
```bash
# 检查DNS解析
dig ukliving.cn CNAME
```

**预期结果**：应该返回API网关的CNAME地址

### 6.2 验证HTTPS访问

**使用浏览器**：
1. 访问：`https://ukliving.cn/health`
2. 应该返回：
```json
{
  "status": "healthy",
  "message": "Property API is running"
}
```
3. 检查浏览器地址栏是否显示绿色锁图标（SSL证书有效）

**使用命令行**：
```bash
curl https://ukliving.cn/health
```

### 6.3 测试API接口

```bash
# 测试房产列表接口
curl https://ukliving.cn/api/properties?page=1&limit=10
```

应该返回房产列表JSON数据。

### 6.4 测试小程序

1. **使用微信开发者工具**：
   - 打开小程序项目
   - 点击"编译"
   - 测试各个页面功能
   - 检查控制台是否有网络错误

2. **检查API调用**：
   - 查看"Network"面板
   - 确认请求地址是 `https://ukliving.cn/api/...`
   - 确认请求成功（状态码200）

3. **测试关键功能**：
   - ✅ 房产列表加载
   - ✅ 房产详情显示
   - ✅ 图片加载
   - ✅ 筛选功能
   - ✅ 价格排序

---

## ✅ 完成检查清单

### SSL证书
- [ ] SSL证书已申请
- [ ] 证书状态为"已签发"
- [ ] 证书域名正确（`ukliving.cn`）

### DNS解析
- [ ] DNS记录已添加（CNAME）
- [ ] 记录值指向API网关CNAME地址
- [ ] 使用 `nslookup` 可以查询到CNAME记录
- [ ] DNS解析已生效

### API网关
- [ ] 域名已在API网关中绑定
- [ ] SSL证书已绑定到API网关
- [ ] 路径映射配置正确
- [ ] 可以通过HTTPS访问健康检查接口

### 小程序代码
- [ ] `miniprogram/app.js` 中的 `apiBaseUrl` 已更新为 `https://ukliving.cn`
- [ ] 没有其他地方硬编码临时域名
- [ ] 代码已保存

### 微信小程序后台
- [ ] request合法域名已配置：`https://ukliving.cn`
- [ ] downloadFile合法域名已配置（包括COS域名）
- [ ] 配置已保存

### 功能测试
- [ ] 健康检查接口正常：`https://ukliving.cn/health`
- [ ] API接口正常：`https://ukliving.cn/api/properties`
- [ ] 小程序可以正常加载房产列表
- [ ] 小程序可以正常显示房产详情
- [ ] 图片可以正常加载
- [ ] SSL证书有效（浏览器显示绿色锁）

---

## 🔧 常见问题

### Q1: SSL证书签发很慢怎么办？
**答**：
- 通常0.5-2小时，最长可能24小时
- 检查域名验证是否通过
- 如果超过24小时，联系腾讯云客服

### Q2: DNS解析不生效？
**答**：
- 等待10分钟-2小时
- 检查DNS记录配置是否正确
- 使用 `nslookup` 验证
- 清除本地DNS缓存：`ipconfig /flushdns`（Windows）

### Q3: 绑定域名时提示域名未备案？
**答**：
- 确认域名备案状态：https://beian.miit.gov.cn
- 如果已备案，可能需要等待备案信息同步（24小时内）
- 确认备案主体与腾讯云账号一致

### Q4: 小程序提示"不在以下合法域名列表中"？
**答**：
- 确认已在微信小程序后台配置服务器域名
- 配置后重启微信开发者工具
- 检查域名是否输入正确（必须包含 `https://`）

### Q5: 使用主域名还是子域名？
**答**：
- **主域名**（`ukliving.cn`）：更简洁，用户友好
- **子域名**（`api.ukliving.cn`）：结构清晰，便于管理

**建议**：如果没有其他用途，直接使用主域名 `ukliving.cn`。

---

## 📞 需要帮助？

如果遇到问题：
1. 检查上述检查清单是否全部完成
2. 查看API网关日志
3. 查看云函数日志
4. 联系腾讯云客服

---

## 🎯 总结

### 关键步骤
1. ✅ 申请SSL证书（先完成）
2. ✅ 配置DNS解析（CNAME记录）
3. ✅ 在API网关绑定域名
4. ✅ 更新小程序代码中的 `apiBaseUrl`
5. ✅ 在微信小程序后台配置服务器域名
6. ✅ 测试验证所有功能

### 切换后的访问地址
- **临时域名**（旧）：`https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com`
- **正式域名**（新）：`https://ukliving.cn`

**完成以上步骤后，即可使用正式域名访问API！** 🎉
