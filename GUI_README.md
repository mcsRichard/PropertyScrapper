# 房产数据GUI使用指南

## 功能特性

✅ **数据浏览**: 在图形界面中浏览所有房产数据  
✅ **图片存储**: 自动下载并存储房产图片到本地  
✅ **图片显示**: 实时显示房产图片  
✅ **搜索功能**: 快速搜索房产  
✅ **详情查看**: 查看完整的房产信息（中英文描述）  

## 安装依赖

```bash
pip install pillow requests
```

## 使用方法

### 1. 启动GUI应用

```bash
python property_gui.py
```

### 2. 加载数据

- GUI启动后会自动加载 `properties.csv` 文件
- 或点击"加载CSV"按钮选择其他CSV文件

### 3. 浏览房产

- 在左侧列表中选择房产
- 使用"上一个"和"下一个"按钮导航
- 或直接点击列表中的房产

### 4. 搜索房产

- 在搜索框中输入关键词
- 实时过滤房产列表

### 5. 下载图片

- 点击"下载所有图片"按钮
- 图片将保存到 `property_images/` 目录
- 下载完成后，图片会在GUI中自动显示

### 6. 查看详情

- 右侧显示房产图片（下载后）
- 下方显示完整信息：
  - 标题和价格
  - 中文描述
  - 英文描述
  - 图片URL和详情链接

## 目录结构

```
PropertyScrapper/
├── property_gui.py          # GUI主程序
├── properties.csv            # 房产数据
├── property_images/          # 图片存储目录（自动创建）
└── GUI_README.md            # 使用指南
```

## 功能说明

### 图片存储

- 图片存储在 `property_images/` 目录
- 自动去重，已下载的图片不会重复下载
- 支持断点续传

### 搜索功能

- 支持标题搜索
- 支持价格搜索
- 实时过滤结果

### 数据格式

GUI支持标准CSV格式，包含以下字段：
- `title`: 房产标题
- `price`: 价格
- `description`: 英文描述
- `description_chinese`: 中文描述
- `image`: 图片URL
- `url`: 详情链接

## 键盘快捷键

- `↑` / `↓`: 选择上一个/下一个房产
- `Enter`: 打开选中房产详情

## 常见问题

### Q: 图片显示不出来？
**A:** 点击"下载所有图片"按钮，等待下载完成。

### Q: 如何清除已下载的图片？
**A:** 点击"清除图片缓存"按钮。

### Q: 支持哪些图片格式？
**A:** 支持 JPG, PNG, GIF 等常见格式。

### Q: 图片存储在哪个位置？
**A:** 图片存储在 `property_images/` 目录下。

## 注意事项

1. 首次运行需要下载图片，可能需要一些时间
2. 确保网络连接正常
3. 图片存储会占用磁盘空间
4. 建议定期清理不需要的图片

## 截图说明

GUI界面分为三个区域：
- **左侧**: 房产列表和搜索框
- **右上**: 房产图片显示区域
- **右下**: 房产详细信息

## 更新日志

### v1.0.0 (2024-11)
- ✅ 初始版本
- ✅ 支持CSV数据加载
- ✅ 支持图片下载和显示
- ✅ 支持搜索功能
- ✅ 支持列表导航

## 技术支持

如有问题，请检查：
1. 是否正确安装了依赖 `pillow` 和 `requests`
2. CSV文件格式是否正确
3. 网络连接是否正常



