# 如何获取腾讯云API密钥

## 📍 快速访问

直接访问API密钥页面：
```
https://console.cloud.tencent.com/cam/capi
```

## 🔑 详细步骤

### 方式1: 通过头像菜单

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 将鼠标移动到右上角 **头像**
3. 点击弹出的下拉菜单中的 **访问管理**
4. 在左侧菜单选择 **API密钥管理**
5. 查看或创建密钥

### 方式2: 通过搜索

1. 登录腾讯云控制台
2. 在顶部搜索框输入：**API密钥管理**
3. 点击搜索结果进入页面
4. 查看或创建密钥

### 方式3: 手动导航

1. 登录腾讯云控制台
2. 点击顶部导航栏的 **云产品**
3. 在 **管理与审计** 分类下找到 **访问管理**
4. 进入后在左侧菜单选择 **API密钥管理**
5. 查看或创建密钥

## 📸 界面位置说明

```
┌────────────────────────────────────────────┐
│  腾讯云控制台                              │
├────────────────────────────────────────────┤
│                                     [头像] │ ← 点击这里
│                                           ▼ │
│                                      ┌────┐ │
│                                      │访问管理 │
│                                      │API密钥│
│                                      └────┘ │
└────────────────────────────────────────────┘
```

或者：

```
┌────────────────────────────────────────────┐
│  搜索框: [___________________] [搜索]       │ ← 输入"API密钥"
├────────────────────────────────────────────┤
│  搜索结果:                                 │
│  ✓ API密钥管理 （访问管理）                │ ← 点击这个
└────────────────────────────────────────────┘
```

## 🎯 创建新密钥（如需要）

如果没有API密钥：

1. 在 **API密钥管理** 页面，点击 **新建密钥**
2. 选择密钥用途（选"子账号API密钥"或直接确认）
3. 系统会生成新的密钥对
4. **立即保存** `SecretId` 和 `SecretKey`

⚠️ **重要提示**：
- SecretKey 只显示一次，请立即复制保存
- 建议将密钥保存在 `.env` 文件中，不要提交到Git
- 如果忘记SecretKey，需要重新创建

## 📝 密钥信息

### SecretId（公钥）
- 格式：`AKIDxxxxx...`
- 用途：API调用时作为身份标识
- 特点：可以分享，不影响安全

### SecretKey（私钥）
- 格式：`xxxxxxxx...`
- 用途：用于签名，验证请求合法性
- 特点：**必须保密**，不要泄露给他人

## ✅ 验证密钥

获取密钥后，在项目根目录创建 `.env` 文件：

```env
# 腾讯云COS配置
COS_SECRET_ID=你的SecretId
COS_SECRET_KEY=你的SecretKey
COS_REGION=ap-shanghai
COS_BUCKET=property-images
COS_DOMAIN=
```

然后测试：

```python
python Scrapper.py
```

如果配置正确，会看到：
```
[COS] 正在上传到: property-images/2024/11/15/image.jpg
[COS] 上传成功: https://...
```

## 🆘 常见问题

### Q1: 找不到"访问管理"？

**A**: 直接访问：https://console.cloud.tencent.com/cam/capi

### Q2: 页面显示"权限不足"？

**A**: 
1. 确保使用的是主账号或有权限的子账号
2. 联系管理员分配API密钥管理权限

### Q3: 密钥创建后看不到了？

**A**: 
- SecretKey 只显示一次创建时
- 如果需要重新获取，只能删除旧密钥并创建新密钥
- 建议立即保存到 `.env` 文件

### Q4: 密钥安全问题？

**A**: 
- 不要将密钥提交到Git仓库
- 将 `.env` 添加到 `.gitignore`
- 如果密钥泄露，立即在控制台禁用并重新创建

## 🔒 安全建议

1. ✅ 将密钥保存在 `.env` 文件
2. ✅ 添加 `.env` 到 `.gitignore`
3. ✅ 不要截图或分享密钥
4. ✅ 定期更换密钥
5. ✅ 使用子账号并设置最小权限

## 📚 更多资源

- 腾讯云官方文档：https://cloud.tencent.com/document/product/436/12159
- API密钥管理：https://console.cloud.tencent.com/cam/capi
- 访问管理控制台：https://console.cloud.tencent.com/cam

## 🎯 下一步

获取密钥后，请继续：

1. ✅ 创建COS存储桶
2. ✅ 配置 `.env` 文件
3. ✅ 安装依赖：`pip install cos-python-sdk-v5`
4. ✅ 运行爬虫：`python Scrapper.py`

详细配置请参考：`COS_SETUP.md`



