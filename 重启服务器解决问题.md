# 问题解决方案：重启API服务器

## 问题确认

✅ **代码检查完成** - 所有代码都是正确的：
- ✅ 数据库模型有 `listing_type` 字段
- ✅ API代码正确提取 `listing_type` 参数
- ✅ 数据库服务正确应用筛选条件
- ✅ 数据库查询结果正确（28个for_sale，27个for_rent）

❌ **问题原因**：API服务器还在运行旧代码，需要重启

## 解决步骤

### 1. 停止当前API服务器

找到运行API的终端窗口，按 `Ctrl+C` 停止

### 2. 重新启动API服务器

在项目根目录或backend目录执行：

```bash
cd backend
python app.py
```

或者：

```bash
python -m backend.app
```

### 3. 等待服务器启动

看到类似这样的输出：
```
 * Running on http://127.0.0.1:5000
```

### 4. 测试API

打开**新的终端窗口**，运行：

```bash
python test_api_query.py
```

或使用curl：

```bash
# 测试出售房产（应该返回28个）
curl "http://localhost:5000/api/properties?listing_type=for_sale&limit=3"

# 测试出租房产（应该返回27个）
curl "http://localhost:5000/api/properties?listing_type=for_rent&limit=3"
```

### 5. 验证结果

API应该返回：

#### 测试 for_sale
- ✅ total: 28
- ✅ properties[0].listing_type: "for_sale"
- ✅ properties[0].title: "1 bed flat for sale" (或类似的出售标题)

#### 测试 for_rent
- ✅ total: 27
- ✅ properties[0].listing_type: "for_rent"
- ✅ properties[0].title: "2 bed flat to rent" (或类似的出租标题)

## 如果重启后问题仍然存在

### 检查是否有缓存

清除Python缓存：
```bash
# 删除所有 __pycache__ 目录
find . -type d -name "__pycache__" -exec rm -r {} +
```

然后重新启动服务器。

### 检查是否使用了错误的启动命令

确认使用的是：
```bash
python app.py
```

而不是：
```bash
python -c "from app import app; app.run()"  # 错误
```

### 查看服务器终端输出

重启后访问API时，应该看到调试信息：
```
[DEBUG] Request parameters:
  listing_type: for_sale
  page: 1, limit: 5
[DEBUG] Applying listing_type filter: for_sale
[DEBUG] Query result: 28 properties found
```

如果看到 `No listing_type filter applied`，说明参数没有正确传递。

## 快速测试脚本

创建一个文件 `test_after_restart.py`：

```python
# -*- coding: utf-8 -*-
import requests
import json

print("测试API服务器...")

# Test for_sale
r = requests.get('http://localhost:5000/api/properties?listing_type=for_sale&limit=3')
data = r.json()
print(f"\nfor_sale查询:")
print(f"  总数: {data['data']['pagination']['total']}")
print(f"  返回: {len(data['data']['properties'])} 条")
for prop in data['data']['properties'][:3]:
    print(f"  - {prop['listing_type']}: {prop['title']}")

# Test for_rent
r = requests.get('http://localhost:5000/api/properties?listing_type=for_rent&limit=3')
data = r.json()
print(f"\nfor_rent查询:")
print(f"  总数: {data['data']['pagination']['total']}")
print(f"  返回: {len(data['data']['properties'])} 条")
for prop in data['data']['properties'][:3]:
    print(f"  - {prop['listing_type']}: {prop['title']}")
```

运行：
```bash
python test_after_restart.py
```

## 预期输出

如果一切正常，应该看到：

```
for_sale查询:
  总数: 28
  返回: 3 条
  - for_sale: 1 bed flat for sale
  - for_sale: 2 bed flat for sale
  - for_sale: 3 bed terraced house for sale

for_rent查询:
  总数: 27
  返回: 3 条
  - for_rent: 2 bed flat to rent
  - for_rent: 4 bed flat to rent
  - for_rent: 5 bed terraced house to rent
```

## 重要提示

1. **必须完全停止旧服务器** - 确保按 `Ctrl+C` 后进程完全退出
2. **使用正确的启动命令** - 使用 `python app.py`
3. **等待服务器完全启动** - 看到 "Running on http://127.0.0.1:5000"
4. **测试新的终端** - 不要用正在运行服务器的终端测试
5. **检查端口** - 确认没有其他进程占用5000端口

如果还有问题，请查看API服务器终端的输出，看看调试信息显示什么。

