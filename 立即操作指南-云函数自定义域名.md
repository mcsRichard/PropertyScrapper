# 立即操作指南 - 云函数自定义域名配置

## 📊 当前状态确认

✅ **云函数运行正常**：
```
https://1383789184-lebyqyy34m.ap-shanghai.tencentscf.com/health
返回：{"message":"Property API is running","status":"healthy"}
```

✅ **DNS解析正确**：
```
api.ukliving.cn → 1383789184-lebyqyy34m.ap-shanghai.tencentscf.com
```

✅ **SSL证书已准备**：
- 证书域名：`api.ukliving.cn`

❌ **自定义域名未配置**：
- 需要在云函数控制台绑定域名和证书

---

## 🎯 立即操作步骤（10分钟完成）

### 第1步：进入云函数控制台

1. **访问云函数列表**
   - 链接：https://console.cloud.tencent.com/scf/list?rid=4
   - `rid=4` 是上海地域（从你的URL判断）

2. **找到你的云函数**
   - 在函数列表中，根据URL `1383789184-lebyqyy34m` 找到对应的函数
   - 函数名称可能包含：`property-api`、`backend`、或类似名称
   - 点击函数名称进入详情

---

### 第2步：配置自定义域名

#### 2.1 查看触发管理

1. **点击"触发管理"标签**
   - 查看现有的触发器

2. **检查是否有HTTP触发器或Web函数URL**
   - 如果有，记录下来
   - 如果没有，需要创建（见下方）

#### 2.2 创建HTTP触发器（如果需要）

如果没有HTTP触发器：

1. 点击"创建触发器"
2. 配置：
   ```
   触发方式：HTTP 触发器
   路径：/ （或留空）
   方法：ANY
   集成响应：启用
   ```
3. 保存

#### 2.3 配置自定义域名

**方法A：在触发管理中配置**

1. 在"触发管理"页面
2. 找到"自定义域名"或"域名配置"选项
3. 点击"新建"或"绑定域名"

**方法B：通过左侧菜单**

1. 在云函数控制台左侧菜单
2. 找到"自定义域名"（可能在"触发器"或"网络"分类下）
3. 点击"新建自定义域名"

#### 2.4 填写域名配置

**域名信息**：
```
自定义域名：api.ukliving.cn
协议：HTTPS（推荐）或 HTTP & HTTPS
网络类型：公网
地域：自动识别（应该是 ap-shanghai）
```

**SSL证书配置**：
```
证书来源：腾讯云托管证书
选择证书：选择绑定域名为 api.ukliving.cn 的证书
```

**⚠️ 重要**：必须选择正确的证书（`api.ukliving.cn`），不要选 `ukliving.cn`！

**路径配置**：
```
路径：/
函数：选择当前函数（应该自动选中）
版本/别名：$DEFAULT 或 LATEST
```

**高级配置**（可选）：
```
启用CORS：是（小程序需要跨域支持）
```

#### 2.5 保存并等待生效

1. 点击"确定"或"提交"
2. 等待配置生效（通常1-5分钟）
3. 系统可能会提示配置DNS记录

---

### 第3步：确认DNS记录（应该已经正确）

你的DNS记录已经配置正确：
```
类型：CNAME
主机记录：api
记录值：1383789184-lebyqyy34m.ap-shanghai.tencentscf.com
```

**无需修改**，保持现有配置即可。

---

### 第4步：验证配置

等待3-5分钟后，测试访问：

#### 4.1 浏览器测试

访问：
```
https://api.ukliving.cn/health
```

**预期结果**：
```json
{
  "status": "healthy",
  "message": "Property API is running"
}
```

**检查SSL证书**：
- 点击浏览器地址栏的锁图标
- 查看证书信息
- 确认证书域名是 `api.ukliving.cn`
- 确认证书有效

#### 4.2 测试API接口

访问：
```
https://api.ukliving.cn/api/properties?page=1&limit=10
```

应该返回房产列表JSON数据。

---

## 🔍 如果找不到云函数

### 方法1：通过URL搜索

1. 在云函数控制台使用搜索功能
2. 搜索：`1383789184` 或 `lebyqyy34m`
3. 或者搜索可能的函数名称：`property-api`、`backend`

### 方法2：查看所有函数

1. 访问：https://console.cloud.tencent.com/scf/list?rid=4
2. 切换地域为"上海"（ap-shanghai）
3. 查看所有函数列表
4. 点击每个函数，查看"触发管理"
5. 对比访问URL是否匹配

### 方法3：通过部署记录查找

查看项目中的 `scf_deploy` 目录，可能有部署记录或配置文件包含函数名称。

---

## ✅ 完成检查清单

配置完成后，检查以下项目：

### 云函数配置
- [ ] 找到了云函数（函数名称：_______）
- [ ] 函数有HTTP触发器或Web函数URL
- [ ] 配置了自定义域名 `api.ukliving.cn`
- [ ] 选择了正确的SSL证书（`api.ukliving.cn`）
- [ ] 启用了CORS（如果有此选项）

### DNS配置
- [ ] DNS记录类型为 CNAME
- [ ] 主机记录为 `api`
- [ ] 记录值为云函数地址（已正确）

### 访问测试
- [ ] 可以访问 `https://api.ukliving.cn/health`
- [ ] 返回正常JSON数据
- [ ] 浏览器显示绿色锁（SSL证书有效）
- [ ] API接口正常工作

---

## 🆘 常见问题

### Q1: 找不到"自定义域名"配置选项？

**可能原因**：
- 函数类型不对（事件函数无法配置自定义域名）
- 需要先创建HTTP触发器

**解决方法**：
1. 确认函数类型是"Web函数"
2. 如果不是，可能需要重新部署为Web函数
3. 或者创建HTTP触发器

### Q2: 配置了自定义域名，但仍无法访问？

**检查**：
1. 等待3-5分钟让配置生效
2. 确认DNS记录正确
3. 确认SSL证书选择正确
4. 清除浏览器缓存和DNS缓存
5. 查看云函数日志是否有错误

### Q3: SSL证书错误？

**检查**：
1. 确认选择的证书域名是 `api.ukliving.cn`
2. 确认证书状态为"已签发"
3. 如果选错了，重新配置选择正确的证书

### Q4: 函数是事件函数，不是Web函数？

如果函数是事件函数，不支持HTTP访问和自定义域名，需要：
1. 重新部署为Web函数
2. 或者使用API网关（但已停服）
3. 建议参考 `scf_deploy` 目录重新部署

---

## 📞 需要帮助？

完成配置后，请告诉我：

1. **找到云函数了吗？**
   - [ ] 找到了（函数名称：_______）
   - [ ] 没找到（需要帮助）

2. **自定义域名配置成功了吗？**
   - [ ] 成功（已配置）
   - [ ] 失败（遇到什么问题：_______）

3. **访问测试结果如何？**
   - [ ] 成功（可以访问）
   - [ ] 失败（错误信息：_______）

---

## 🔗 相关链接

- **云函数控制台**：https://console.cloud.tencent.com/scf
- **云函数文档**：https://cloud.tencent.com/document/product/583
- **SSL证书管理**：https://console.cloud.tencent.com/ssl
- **DNS管理**：https://console.cloud.tencent.com/cns

---

## 💡 总结

**关键步骤**：
1. ✅ 云函数正常运行（已确认）
2. ✅ DNS解析正确（已配置）
3. ✅ SSL证书已准备（api.ukliving.cn）
4. 🔄 **需要配置**：在云函数控制台绑定自定义域名和SSL证书

完成配置后，`https://api.ukliving.cn` 就可以正常访问了！🎉
