# 云函数部署说明

## 📝 函数代码填写

### 当前阶段（先创建函数用于备案）

在云函数代码编辑器中填写以下代码：

```python
# -*- coding: utf-8 -*-
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'backend'))
from app import create_app
app = create_app()
```

**入口设置**：
- 入口文件：`index.py`
- 入口函数：`app`

---

### 后续部署完整代码

**备案通过后，需要做**：

#### 1. 准备上传包

创建以下目录结构：
```
property-api/
├── index.py              ← 入口文件（上面的代码）
├── backend/              ← 你的整个backend目录
│   ├── app.py
│   ├── config.py
│   ├── requirements.txt
│   ├── api/
│   ├── models/
│   └── utils/
└── requirements.txt      ← 依赖文件（从backend复制）
```

#### 2. 上传方式

**方式1：在线编辑**（简单，但代码多会乱）
- 在云函数编辑器直接粘贴代码
- 不推荐（代码太多）

**方式2：本地上传**（推荐）
- 在云函数控制台
- 点击"本地上传文件夹"
- 选择 `property-api/` 目录
- 上传

**方式3：命令行部署**（高级）
```bash
# 安装Serverless Framework
npm install -g serverless

# 部署
cd property-api
serverless deploy
```

---

## 🔧 当前操作

### 现在：只填写入口代码

**复制这段代码到云函数编辑器**：

```python
# -*- coding: utf-8 -*-
"""
云函数入口
"""
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'backend'))
from app import create_app
app = create_app()
```

**设置**：
- ✅ 入口文件：`index.py`
- ✅ 入口函数：`app`
- ✅ 点击"完成"

**就这么简单！**

---

## ⚠️ 重要提示

### 现在先不要上传完整代码

**原因**：
1. 你现在主要目的是**备案**
2. 只有代码入口就可以完成备案
3. 备案需要7-15天
4. 备案期间再准备完整部署

**备案期间可以做的事**：
- ✅ 本地测试代码
- ✅ 准备部署文件
- ✅ 查看部署文档

---

## 🎯 完整部署流程

### 阶段1：现在（完成备案）

```
1. 创建云函数（Web函数）
2. 填写简单的入口代码
3. 暂不配置HTTP触发器
4. 开始备案
```

### 阶段2：等待备案（7-15天）

```
1. 准备完整的代码包
2. 测试本地功能
3. 准备小程序审核材料
```

### 阶段3：备案通过后（1-2小时）

```
1. 上传完整的backend代码
2. 配置HTTP触发器
3. 配置域名
4. 配置SSL证书
```

---

## 📋 部署检查清单

### 创建时（现在）
- [ ] 函数类型：Web函数
- [ ] 地域：华东（上海）
- [ ] 环境：Python 3.7
- [ ] 入口代码已填写
- [ ] 入口文件：index.py
- [ ] 入口函数：app

### 备案后（7-15天后）
- [ ] 完整代码已上传
- [ ] 环境变量已配置
- [ ] HTTP触发器已配置
- [ ] 域名已绑定
- [ ] SSL证书已配置
- [ ] 测试通过

---

## ✅ 现在立即行动

**在云函数代码编辑器中**：

1. **删除默认代码**
2. **粘贴下面的代码**：

```python
# -*- coding: utf-8 -*-
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'backend'))
from app import create_app
app = create_app()
```

3. **点击"保存"**

**完成！** 🎉

