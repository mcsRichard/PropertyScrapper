# 个人主体小程序客服功能配置说明

## 📋 功能说明

由于个人主体小程序不支持微信官方客服功能，我们实现了**表单提交 + 邮件通知**的替代方案。

## ✅ 已实现功能

1. ✅ 咨询表单页面（小程序前端）
2. ✅ 咨询提交API（后端）
3. ✅ 数据库存储（咨询记录）
4. ✅ 邮件通知功能（可选）
5. ✅ 咨询列表查询API（管理员）

---

## 🔧 配置步骤

### 第一步：初始化数据库

咨询记录表会在启动API时自动创建。如果表已存在，可以手动运行：

```bash
cd backend
python -c "from models.database import create_tables; create_tables()"
```

### 第二步：配置邮件通知（可选但推荐）

#### 2.1 获取邮箱授权码

**QQ邮箱：**
1. 登录QQ邮箱
2. 设置 -> 账户
3. 开启SMTP服务
4. 生成授权码（保存好，用于配置）

**Gmail：**
1. 登录Gmail账户
2. 启用两步验证
3. 生成应用专用密码
4. 使用应用专用密码作为EMAIL_PASSWORD

**其他邮箱：**
- 163邮箱：smtp.163.com
- 126邮箱：smtp.126.com
- 企业邮箱：咨询邮箱服务商

#### 2.2 配置环境变量

编辑 `backend/.env` 文件，添加以下配置：

```bash
# 咨询表单邮件通知配置
EMAIL_ENABLED=True
EMAIL_HOST=smtp.qq.com  # SMTP服务器地址
EMAIL_PORT=587  # SMTP端口（QQ邮箱使用587）
EMAIL_USER=your-email@qq.com  # 发件人邮箱
EMAIL_PASSWORD=your-authorization-code  # 邮箱授权码（不是邮箱密码）
EMAIL_TO=admin@example.com  # 接收通知的管理员邮箱
EMAIL_USE_TLS=True  # 是否使用TLS加密
```

**配置示例（QQ邮箱）：**
```bash
EMAIL_ENABLED=True
EMAIL_HOST=smtp.qq.com
EMAIL_PORT=587
EMAIL_USER=123456789@qq.com
EMAIL_PASSWORD=abcdefghijklmnop  # QQ邮箱授权码
EMAIL_TO=admin@example.com
EMAIL_USE_TLS=True
```

**配置示例（Gmail）：**
```bash
EMAIL_ENABLED=True
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password  # Gmail应用专用密码
EMAIL_TO=admin@example.com
EMAIL_USE_TLS=True
```

#### 2.3 测试邮件发送

启动API服务器后，提交一条咨询测试邮件是否正常发送。

---

### 第三步：配置小程序API地址

确保 `miniprogram/app.js` 中的 `apiBaseUrl` 指向正确的后端地址。

---

## 📝 使用说明

### 用户端（小程序）

1. 用户在房源详情页点击"📝 咨询房源"按钮
2. 跳转到咨询表单页面
3. 填写联系方式（电话必填）和咨询内容
4. 提交表单
5. 显示提交成功提示

### 管理员端

#### 查看咨询记录

**API接口：**
```
GET /api/consultation/list?page=1&limit=20&status=pending
```

**参数：**
- `page`: 页码（默认1）
- `limit`: 每页数量（默认20，最大100）
- `status`: 状态筛选（可选：pending, replied, closed）
- `property_id`: 房源ID筛选（可选）

**返回示例：**
```json
{
  "success": true,
  "data": {
    "consultations": [
      {
        "id": 1,
        "property_id": 123,
        "property_title": "2 Bedroom Flat in London",
        "property_price": "£500,000",
        "name": "张三",
        "phone": "13800138000",
        "email": "user@example.com",
        "message": "我想了解更多详情",
        "status": "pending",
        "created_at": "2024-01-01T10:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 1,
      "pages": 1
    }
  }
}
```

#### 回复咨询

**API接口：**
```
POST /api/consultation/{consultation_id}/reply
```

**请求体：**
```json
{
  "reply_message": "感谢您的咨询，我们会尽快联系您。",
  "status": "replied"  // 或 "closed"
}
```

---

## 📊 数据库表结构

### consultations 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| property_id | Integer | 房源ID（外键） |
| name | String(100) | 用户姓名（可选） |
| phone | String(50) | 联系电话（必填） |
| email | String(100) | 邮箱（可选） |
| wechat | String(100) | 微信号（可选） |
| message | Text | 咨询内容 |
| property_title | String(255) | 房源标题（快照） |
| property_price | String(50) | 房源价格（快照） |
| property_url | String(500) | 房源链接（快照） |
| property_image_url | String(500) | 房源封面图（快照） |
| status | String(20) | 状态：pending/replied/closed |
| reply_message | Text | 回复内容 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |
| replied_at | DateTime | 回复时间 |

---

## 🎯 工作流程

1. **用户提交咨询**
   - 填写表单 -> 提交 -> 保存到数据库
   - 发送邮件通知管理员（如果启用）

2. **管理员接收通知**
   - 收到邮件通知
   - 查看咨询详情（房源信息、用户信息、咨询内容）

3. **管理员回复用户**
   - 通过电话/微信联系用户
   - 在系统中标记已回复

---

## 🔍 故障排查

### 邮件发送失败

**问题：** 收不到邮件通知

**排查步骤：**
1. 检查 `EMAIL_ENABLED` 是否为 `True`
2. 检查邮箱配置是否正确（SMTP服务器、端口）
3. 检查邮箱授权码是否正确（不是邮箱密码）
4. 检查防火墙是否允许SMTP端口
5. 查看后端日志中的错误信息

**常见错误：**
- `535 Error: authentication failed` - 授权码错误
- `Connection refused` - SMTP服务器地址或端口错误
- `Timeout` - 网络问题或防火墙阻止

### 咨询记录未保存

**问题：** 提交咨询后数据库中没有记录

**排查步骤：**
1. 检查数据库连接是否正常
2. 检查数据库表是否已创建
3. 查看后端日志中的错误信息
4. 检查API接口是否正常响应

### 小程序无法提交

**问题：** 小程序提交表单失败

**排查步骤：**
1. 检查API地址配置是否正确
2. 检查网络连接
3. 检查后端服务是否运行
4. 查看小程序控制台错误信息

---

## 💡 最佳实践

### 1. 邮件通知

- ✅ 启用邮件通知，及时接收咨询
- ✅ 设置邮件过滤规则，避免遗漏
- ✅ 定期检查垃圾邮件文件夹

### 2. 咨询管理

- ✅ 及时回复用户咨询（建议24小时内）
- ✅ 标记已处理的咨询（status: replied/closed）
- ✅ 定期清理旧咨询记录

### 3. 用户体验

- ✅ 在表单中提供清晰的填写说明
- ✅ 添加表单验证，避免无效提交
- ✅ 提交成功后给用户明确的反馈

---

## 📚 相关文件

- `backend/models/database.py` - 数据库模型
- `backend/api/consultation.py` - 咨询API
- `backend/utils/email_sender.py` - 邮件发送工具
- `miniprogram/pages/consultation/` - 咨询表单页面
- `miniprogram/utils/api.js` - API调用工具

---

## ✅ 配置检查清单

- [ ] 数据库表已创建
- [ ] 邮件配置已添加（如果需要）
- [ ] 邮件通知已测试
- [ ] 小程序API地址已配置
- [ ] 咨询表单已测试
- [ ] 管理员已了解如何查看咨询记录

---

**配置完成后，个人主体小程序即可使用咨询表单功能！** 🎉

