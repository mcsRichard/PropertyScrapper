# 完整导入流程 - CSV到数据库

## 📋 两步流程

### 步骤1: 爬取数据到CSV

```bash
python Scrapper.py --url "https://www.zoopla.co.uk/to-rent/property/london/sw7/knightsbridge-south-kensington/?q=SW7&search_source=to-rent" --output rental_properties.csv --max-per-property 5
```

**结果**：
- ✅ 爬取房产数据
- ✅ 上传图片到COS（如果启用）
- ✅ 保存到 `rental_properties.csv`
- ❌ **不会写入数据库**

---

### 步骤2: 将CSV导入数据库

#### 选项A：追加数据（推荐）

```bash
python import_to_db.py --file rental_properties.csv
```

**效果**：
- ✅ 保留数据库中已有数据
- ✅ 新增CSV中的数据
- ✅ 如果URL重复，会插入新记录（可能产生重复）

---

#### 选项B：清空后导入

```bash
python import_to_db.py --file rental_properties.csv --clear
```

**效果**：
- ❌ **删除数据库中所有已有数据**
- ✅ 导入CSV中的所有数据
- ⚠️ **注意**：会清空现有数据！

---

## 🎯 推荐操作

### 第一次导入（出租房产）

```bash
# 1. 爬取数据
python Scrapper.py --url "https://www.zoopla.co.uk/to-rent/property/london/sw7/knightsbridge-south-kensington/?q=SW7&search_source=to-rent" --output rental_properties.csv --max-per-property 5

# 2. 追加到数据库（保留已有数据）
python import_to_db.py --file rental_properties.csv
```

### 更新数据（替换所有）

```bash
# 1. 爬取最新数据
python Scrapper.py --url "..." --output rental_properties.csv --max-per-property 5

# 2. 清空表后导入（⚠️ 会删除旧数据）
python import_to_db.py --file rental_properties.csv --clear
```

---

## 📊 数据流程

```
Zoopla网站
    ↓
Scrapper.py（爬取）
    ↓
rental_properties.csv（CSV文件）
    ↓
import_to_db.py（导入）
    ↓
数据库（TDSQL-C）
    ↓
小程序（显示）
```

---

## ⚠️ 注意事项

### 1. 数据重复

`import_to_db.py` 默认**不会去重**：
- 如果同一个URL多次导入，会插入多条记录
- 建议在数据库中设置 `url` 字段为唯一索引

### 2. listing_type字段

CSV中的 `listing_type` 字段会自动识别：
- `to-rent` URL → `listing_type = 'for_rent'`
- `for-sale` URL → `listing_type = 'for_sale'`

### 3. 图片URL

- 如果启用了COS，图片会先上传到COS
- CSV中的 `image_url` 字段包含COS的URL
- 导入时会使用COS的URL

---

## 🔍 检查导入结果

### 查看CSV文件

```bash
# 查看CSV前几行
head -5 rental_properties.csv
```

### 检查数据库

```bash
# 使用Python脚本检查
python test_db.py

# 或者直接查询数据库
# SELECT COUNT(*) FROM properties WHERE listing_type = 'for_rent';
```

---

## ✅ 总结

**回答你的问题**：

❌ **不会**，`Scrapper.py` 只保存到CSV文件

✅ **需要再运行**：
```bash
python import_to_db.py --file rental_properties.csv
```

才会将数据存入数据库。

