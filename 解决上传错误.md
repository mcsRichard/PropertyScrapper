# 解决图片上传错误

## ❌ 错误信息

```
[COS] SDK not available, fallback to requests
[COS] 上传失败: 403
```

## 🔍 问题原因

1. **未安装COS SDK** - 缺少 `cos-python-sdk-v5`
2. **使用fallback方案** - 使用requests方式需要复杂的签名
3. **403权限错误** - 未正确签名或权限不足

## ✅ 解决方案

### 方法1: 安装COS SDK（推荐）

```bash
pip install cos-python-sdk-v5
```

然后重新运行：

```bash
python Scrapper.py
```

### 方法2: 检查配置

确认 `.env` 文件配置正确：

```env
# backend/.env
COS_SECRET_ID=AKIDxxxxx          # 确保填写正确
COS_SECRET_KEY=xxxxx             # 确保填写正确
COS_REGION=ap-shanghai          # 确保与存储桶地域一致
COS_BUCKET=property-images      # 确保与存储桶名称一致
COS_DOMAIN=                     # 可以留空
```

### 方法3: 临时禁用COS上传（测试用）

如果暂时不需要上传到COS，可以修改 `Scrapper.py`：

```python
# 在Scrapper.py开头，添加这行
COS_ENABLED = False  # 临时禁用
```

图片将保存原始URL，不会上传到COS。

---

## 🛠️ 详细检查步骤

### 1. 检查SDK是否安装

```bash
pip list | grep cos
```

应该看到：
```
cos-python-sdk-v5   1.9.25
```

如果没有，安装：

```bash
pip install cos-python-sdk-v5
```

### 2. 检查环境变量

```bash
# Windows PowerShell
cd backend
cat .env  # 如果不存在，从 env_example.txt 复制

# Linux/Mac
cat backend/.env
```

确保这些值都不为空：
- `COS_SECRET_ID`
- `COS_SECRET_KEY`
- `COS_REGION`
- `COS_BUCKET`

### 3. 测试上传

运行爬虫：

```bash
python Scrapper.py
```

成功输出应该类似：

```
[INFO] COS uploader enabled
[COS] 正在下载: https://lid.zoocdn.com/...
[COS] 正在上传到: property-images/2024/11/15/image.jpg
[COS] 上传成功: https://property-images-xxx.cos.ap-shanghai.myqcloud.com/...
```

---

## 🔧 完整修复流程

### 步骤1: 安装依赖

```bash
pip install cos-python-sdk-v5
```

### 步骤2: 确认配置

检查 `backend/.env` 文件是否存在且配置正确：

```env
# 必须正确填写
COS_SECRET_ID=AKIDxxxxx
COS_SECRET_KEY=xxxxx
COS_REGION=ap-shanghai
COS_BUCKET=property-images
COS_DOMAIN=
```

### 步骤3: 验证配置

```bash
cd backend
python -c "from config import Config; print(Config.COS_BUCKET)"
```

应该输出：`property-images`（或你的存储桶名称）

### 步骤4: 运行爬虫

```bash
python Scrapper.py
```

---

## 📋 常见问题

### Q1: 安装后还是报错？

**检查**：
1. 确认在正确的Python环境安装
2. 如果使用虚拟环境，先激活
3. 检查import路径是否正确

**解决**：
```bash
# 确保在项目根目录
cd D:\Projects\PropertyScrapper\PropertyScrapper
pip install cos-python-sdk-v5
```

### Q2: 仍然是403错误？

**可能原因**：
1. SecretId/SecretKey错误
2. 存储桶权限设置错误
3. 地域配置不匹配

**解决**：
1. 重新检查`.env`配置
2. 确认存储桶设置为"公有读私有写"
3. 检查COS控制台确认存储桶名称和地域

### Q3: 不想上传到COS？

**临时方案**：修改代码禁用COS

在 `Scrapper.py` 中添加：

```python
COS_ENABLED = False
```

这样会跳过上传，保留原始Zoopla图片URL。

---

## 🎯 推荐操作

**最简单的方法**：

1. 安装SDK：
```bash
pip install cos-python-sdk-v5
```

2. 确认配置正确

3. 重新运行：
```bash
python Scrapper.py
```

---

## ✅ 验证修复

运行爬虫后，检查输出：

**成功标志**：
```
[INFO] COS uploader enabled
[COS] 正在上传到: ...
[COS] 上传成功: https://...
```

**如果还是失败**，请提供完整的错误信息。


