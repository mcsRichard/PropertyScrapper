# 微信小程序客服功能开发计划

## 📋 需求概述

用户在小程序里可以直接发送信息给开发者微信，包括：
- 房源ID
- 标题
- 封面图
- 引用链接

开发者端直接用微信客服后台回复，用户不用加好友，也不离开小程序。

---

## 🎯 技术方案

### 方案选择：微信官方客服消息API

微信小程序提供了官方的客服消息功能，通过 `wx.openCustomerServiceConversation` API实现，这是最适合的方案：

**优点：**
- ✅ 官方支持，稳定可靠
- ✅ 用户无需添加好友，直接在小程序内对话
- ✅ 开发者可在微信客服后台统一回复
- ✅ 支持发送文本、图片、链接卡片等多种消息类型
- ✅ 可以携带会话参数（房源信息）

**限制：**
- ⚠️ 需要小程序认证（个人主体无法使用，需要企业主体）
- ⚠️ 需要在微信公众平台配置客服人员
- ⚠️ 客服消息有48小时有效期限制

---

## 📊 复杂度评估

### 整体复杂度：⭐⭐⭐☆☆（中等）

| 模块 | 复杂度 | 工作量 | 备注 |
|------|--------|--------|------|
| 小程序前端调用 | ⭐⭐☆☆☆ | 1-2小时 | API调用较简单 |
| 携带房源参数 | ⭐⭐⭐☆☆ | 2-3小时 | 需要格式化消息内容 |
| 微信后台配置 | ⭐⭐☆☆☆ | 0.5小时 | 需要在后台手动配置 |
| 后端回调（可选） | ⭐⭐⭐⭐☆ | 4-6小时 | 需要处理消息回调 |
| 消息记录存储（可选） | ⭐⭐⭐☆☆ | 2-3小时 | 数据库设计 |

**总计预估时间：**
- **最小实现（仅前端）：** 3-5小时
- **完整实现（含后端）：** 8-12小时

---

## 🚀 开发计划

### 阶段一：基础功能实现（最小MVP）

#### 1.1 小程序前端实现

**文件修改：**
- `miniprogram/pages/detail/detail.js` - 添加客服按钮事件
- `miniprogram/pages/detail/detail.wxml` - 添加客服按钮UI
- `miniprogram/pages/detail/detail.wxss` - 样式调整

**实现步骤：**

1. **在详情页添加"咨询客服"按钮**
   - 位置：操作按钮区域
   - 样式：主按钮样式，突出显示

2. **实现客服消息发送功能**
   - 使用 `wx.openCustomerServiceConversation` API
   - 通过 `sessionFrom` 参数传递房源信息
   - 构造格式化的消息内容

**代码示例：**
```javascript
// 打开客服会话，携带房源信息
openCustomerService() {
  const property = this.data.property
  
  // 构造客服消息内容
  const sessionFrom = `property_${property.id}`
  
  // 构造消息文本内容（包含房源信息）
  const messageText = this.buildPropertyMessage(property)
  
  // 打开客服会话
  wx.openCustomerServiceConversation({
    sessionFrom: sessionFrom,
    success: (res) => {
      console.log('客服会话打开成功', res)
      // 注意：小程序客服API不支持直接发送初始消息
      // 需要引导用户发送消息，或使用客服消息推送功能
    },
    fail: (err) => {
      console.error('客服会话打开失败', err)
      wx.showToast({
        title: '打开客服失败',
        icon: 'none'
      })
    }
  })
  
  // 注意：如果需要发送初始消息，需要在后端配置客服消息推送
  // 或者提示用户主动发送消息
}

// 构造房源消息内容
buildPropertyMessage(property) {
  return `我想咨询这套房源：
  
📍 ${property.title}
💰 ${property.price}
🏠 ${property.bedrooms || '-'}室 ${property.bathrooms || '-'}卫
📍 ${property.location || ''} ${property.postcode || ''}
🔗 房源ID: ${property.id}

更多详情：${property.url || ''}`
}
```

**注意：**
- `wx.openCustomerServiceConversation` 只能打开客服会话，不能直接发送消息
- 如需自动发送初始消息，需要在后端配置客服消息推送

---

### 阶段二：优化用户体验（推荐实现）

#### 2.1 添加消息提示

在打开客服前，给用户提示，引导用户发送消息：

```javascript
openCustomerService() {
  wx.showModal({
    title: '联系客服',
    content: '客服将为您详细介绍这套房源，请在对话框内发送消息开始咨询',
    confirmText: '打开客服',
    cancelText: '取消',
    success: (res) => {
      if (res.confirm) {
        this.openCustomerServiceConversation()
      }
    }
  })
}
```

#### 2.2 格式化房源信息展示

在客服会话中，以卡片形式展示房源信息：

```javascript
// 构造客服消息卡片内容
buildPropertyCard(property) {
  // 由于小程序客服API限制，无法直接发送卡片
  // 但可以通过文本格式优化展示
  const lines = [
    '━━━━━━━━━━━━━━━━━━━━',
    `🏠 ${property.title}`,
    `━━━━━━━━━━━━━━━━━━━━`,
    `💰 价格：${property.price}`,
    `📍 位置：${property.location || ''} ${property.postcode || ''}`,
    `🏡 户型：${property.bedrooms || '-'}室 ${property.bathrooms || '-'}卫`,
    `📐 面积：${property.area || '-'}`,
    `🏷️ 类型：${property.property_type || '-'}`,
    `━━━━━━━━━━━━━━━━━━━━`,
    `📋 房源ID：${property.id}`,
    `🔗 详情链接：${property.url || '暂无'}`,
    `━━━━━━━━━━━━━━━━━━━━`
  ]
  return lines.join('\n')
}
```

---

### 阶段三：后端客服消息推送（高级功能）

如果需要自动发送初始消息，需要在后端配置客服消息推送。

#### 3.1 微信公众平台配置

1. **配置服务器URL**
   - 登录微信公众平台
   - 开发 -> 开发管理 -> 开发设置
   - 消息推送 -> 配置服务器URL

2. **配置客服人员**
   - 微信公众平台 -> 功能 -> 客服功能
   - 添加客服人员（绑定微信账号）
   - 设置客服工作状态

#### 3.2 后端实现客服消息推送API

**文件创建：**
- `backend/api/customer_service.py` - 客服消息API

**实现步骤：**

1. **接收小程序事件推送**
   - 监听 `user_enter_tempsession` 事件
   - 获取用户openid和sessionFrom参数

2. **解析房源信息**
   - 从sessionFrom中提取房源ID
   - 查询房源详细信息

3. **发送客服消息**
   - 调用微信客服消息API
   - 发送格式化的房源信息
   - 支持发送文本、图片、链接等

**代码结构：**
```python
# backend/api/customer_service.py
from flask import Blueprint, request, jsonify
import requests
import json

customer_service_bp = Blueprint('customer_service', __name__, url_prefix='/api/customer-service')

@customer_service_bp.route('/message', methods=['POST'])
def send_customer_message():
    """发送客服消息"""
    # 1. 接收微信推送的事件
    # 2. 解析房源ID
    # 3. 查询房源信息
    # 4. 发送客服消息
    pass

@customer_service_bp.route('/callback', methods=['POST'])
def wechat_callback():
    """接收微信消息推送回调"""
    # 处理用户进入客服会话事件
    pass
```

**依赖：**
- 需要获取微信access_token
- 需要处理微信消息加解密
- 需要使用微信客服消息API

---

## 📝 详细实现步骤

### 步骤1：微信公众平台配置（必须）

1. **登录微信公众平台**
   - 访问：https://mp.weixin.qq.com
   - 使用小程序管理员账号登录

2. **开通客服功能**
   - 功能 -> 客服功能
   - 点击"开通"（如果没有开通）
   - 添加客服人员（绑定微信账号）
   - 设置客服工作状态为"在线"

3. **配置客服消息推送（可选）**
   - 开发 -> 开发管理 -> 开发设置
   - 消息推送 -> 启用
   - 配置服务器URL（需要HTTPS）
   - 配置Token和EncodingAESKey

**注意：**
- 个人主体小程序**不支持**客服功能
- 需要企业主体或个体工商户
- 如果是个人开发者，可以考虑使用其他方案

---

### 步骤2：小程序前端实现

#### 2.1 修改详情页添加客服按钮

**文件：`miniprogram/pages/detail/detail.wxml`**

```xml
<!-- 操作按钮 -->
<view class="action-buttons">
  <button class="btn-primary" bindtap="viewOriginalLink">查看原网页</button>
  <button class="btn-customer-service" bindtap="openCustomerService">
    💬 咨询客服
  </button>
</view>
```

**文件：`miniprogram/pages/detail/detail.wxss`**

```css
.btn-customer-service {
  background-color: #07c160;
  color: #fff;
  margin-top: 20rpx;
}

.btn-customer-service::after {
  border: none;
}
```

#### 2.2 实现客服功能JS

**文件：`miniprogram/pages/detail/detail.js`**

添加方法：
```javascript
/**
 * 打开客服会话
 */
openCustomerService() {
  const property = this.data.property
  
  if (!property || !property.id) {
    wx.showToast({
      title: '房源信息不完整',
      icon: 'none'
    })
    return
  }
  
  // 构造sessionFrom参数（用于识别房源）
  const sessionFrom = `property_${property.id}`
  
  // 打开客服会话
  wx.openCustomerServiceConversation({
    sessionFrom: sessionFrom,
    success: (res) => {
      console.log('[客服] 会话打开成功', res)
      
      // 提示用户发送消息
      wx.showToast({
        title: '请在对话框发送消息',
        icon: 'none',
        duration: 2000
      })
      
      // 可选：复制房源信息到剪贴板，方便用户粘贴
      this.copyPropertyInfoToClipboard(property)
    },
    fail: (err) => {
      console.error('[客服] 会话打开失败', err)
      
      // 错误处理
      let errorMsg = '打开客服失败'
      if (err.errMsg) {
        if (err.errMsg.includes('not support')) {
          errorMsg = '当前版本不支持客服功能，请更新微信'
        } else if (err.errMsg.includes('not configured')) {
          errorMsg = '客服功能未配置，请联系管理员'
        }
      }
      
      wx.showModal({
        title: '提示',
        content: errorMsg,
        showCancel: false
      })
    }
  })
},

/**
 * 复制房源信息到剪贴板（方便用户粘贴到客服对话框）
 */
copyPropertyInfoToClipboard(property) {
  const message = this.buildPropertyMessage(property)
  
  wx.setClipboardData({
    data: message,
    success: () => {
      // 可选：提示用户已复制
      // wx.showToast({
      //   title: '房源信息已复制',
      //   icon: 'success'
      // })
    }
  })
},

/**
 * 构造房源消息内容
 */
buildPropertyMessage(property) {
  const lines = [
    '我想咨询这套房源：',
    '',
    `📍 ${property.title || '无标题'}`,
    `💰 ${property.price || '价格面议'}`,
    `🏠 ${property.bedrooms || '-'}室 ${property.bathrooms || '-'}卫`,
    `📍 ${property.location || ''} ${property.postcode || ''}`,
    property.area ? `📐 面积：${property.area}` : '',
    property.property_type ? `🏷️ 类型：${property.property_type}` : '',
    '',
    `📋 房源ID：${property.id}`,
    property.url ? `🔗 详情：${property.url}` : ''
  ]
  
  return lines.filter(line => line).join('\n')
}
```

---

### 步骤3：优化用户体验（推荐）

#### 3.1 添加客服按钮到首页（可选）

如果需要在列表页也能咨询，可以在首页添加快捷入口。

#### 3.2 添加客服入口统计（可选）

记录用户点击客服按钮的行为，便于分析：

```javascript
openCustomerService() {
  // 记录统计（如果有统计系统）
  // this.trackEvent('customer_service_click', {
  //   property_id: this.data.property.id
  // })
  
  // ... 原有代码
}
```

---

### 步骤4：后端客服消息推送（可选，高级功能）

如果需要自动发送初始消息，需要实现后端推送。

#### 4.1 安装依赖

```bash
pip install wechatpy
```

#### 4.2 创建客服消息API

**文件：`backend/api/customer_service.py`**

```python
# backend/api/customer_service.py
from flask import Blueprint, request, jsonify
from wechatpy import WeChatClient
from wechatpy.exceptions import WeChatClientException
from models.database import get_db
from utils.database import PropertyService
from config import Config
import logging

customer_service_bp = Blueprint('customer_service', __name__, url_prefix='/api/customer-service')

# 微信配置（需要在小程序后台获取）
WECHAT_APPID = Config.WECHAT_APPID  # 需要在config.py中添加
WECHAT_APPSECRET = Config.WECHAT_APPSECRET  # 需要在config.py中添加

# 初始化微信客户端
wechat_client = WeChatClient(WECHAT_APPID, WECHAT_APPSECRET)

@customer_service_bp.route('/send-message', methods=['POST'])
def send_customer_message():
    """发送客服消息"""
    try:
        data = request.get_json()
        openid = data.get('openid')
        property_id = data.get('property_id')
        
        if not openid or not property_id:
            return jsonify({
                'success': False,
                'error': 'Missing openid or property_id'
            }), 400
        
        # 查询房源信息
        db = next(get_db())
        property_service = PropertyService(db)
        property_obj = property_service.get_property_by_id(property_id)
        
        if not property_obj:
            return jsonify({
                'success': False,
                'error': 'Property not found'
            }), 404
        
        # 构造消息内容
        message_text = build_property_message(property_obj)
        
        # 发送客服消息
        wechat_client.message.send_text(
            openid,
            message_text
        )
        
        return jsonify({
            'success': True,
            'message': 'Message sent successfully'
        })
        
    except WeChatClientException as e:
        logging.error(f'WeChat API error: {e}')
        return jsonify({
            'success': False,
            'error': str(e)
        }), 500
    except Exception as e:
        logging.error(f'Error sending customer message: {e}')
        return jsonify({
            'success': False,
            'error': str(e)
        }), 500

def build_property_message(property_obj):
    """构造房源消息内容"""
    lines = [
        '您好！感谢您的咨询。',
        '',
        f'📍 {property_obj.title}',
        f'💰 {property_obj.price}',
        f'🏠 {property_obj.bedrooms or "-"}室 {property_obj.bathrooms or "-"}卫',
        f'📍 {property_obj.location or ""} {property_obj.postcode or ""}',
    ]
    
    if property_obj.area:
        lines.append(f'📐 面积：{property_obj.area}')
    
    if property_obj.property_type:
        lines.append(f'🏷️ 类型：{property_obj.property_type}')
    
    lines.extend([
        '',
        f'📋 房源ID：{property_obj.id}',
        '如有更多问题，欢迎继续咨询！'
    ])
    
    return '\n'.join(lines)

@customer_service_bp.route('/callback', methods=['POST', 'GET'])
def wechat_callback():
    """接收微信消息推送回调"""
    # 处理微信服务器推送的事件
    # 需要实现消息加解密和事件处理
    # 参考：https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/customer-message/customerServiceMessage.send.html
    pass
```

#### 4.3 配置微信参数

**文件：`backend/config.py`**

```python
# 微信小程序配置
WECHAT_APPID = os.getenv('WECHAT_APPID', '')
WECHAT_APPSECRET = os.getenv('WECHAT_APPSECRET', '')
```

---

## 🔧 技术难点和解决方案

### 难点1：小程序客服API限制

**问题：**
- `wx.openCustomerServiceConversation` 只能打开会话，不能直接发送消息
- 无法直接发送图片或链接卡片

**解决方案：**
1. **方案A（简单）：** 引导用户主动发送消息，并在打开客服前复制房源信息到剪贴板
2. **方案B（复杂）：** 实现后端客服消息推送，监听用户进入客服事件，自动发送消息

### 难点2：个人主体小程序限制

**问题：**
- 个人主体小程序不支持客服功能
- 需要企业主体或个体工商户

**解决方案：**
1. 注册企业主体小程序（需要营业执照）
2. 或使用其他替代方案（如公众号、表单等）

### 难点3：客服消息推送实现

**问题：**
- 需要配置服务器URL（HTTPS）
- 需要处理消息加解密
- 需要处理access_token刷新

**解决方案：**
1. 使用第三方库（如wechatpy）简化开发
2. 配置HTTPS服务器（可使用腾讯云、阿里云等）
3. 实现token缓存和自动刷新机制

---

## 📋 开发 checklist

### 基础功能
- [ ] 微信公众平台开通客服功能
- [ ] 添加客服人员
- [ ] 小程序前端添加客服按钮
- [ ] 实现打开客服会话功能
- [ ] 测试客服功能（需要真实小程序环境）

### 优化功能
- [ ] 添加房源信息格式化显示
- [ ] 优化客服按钮UI
- [ ] 添加错误提示
- [ ] 添加用户引导

### 高级功能（可选）
- [ ] 配置服务器URL
- [ ] 实现客服消息推送
- [ ] 实现消息回调处理
- [ ] 添加消息记录存储
- [ ] 实现客服消息统计

---

## 💡 最佳实践建议

### 1. 用户体验优化

- **清晰引导：** 在打开客服前，给用户明确的提示
- **信息准备：** 自动复制房源信息，方便用户粘贴
- **错误处理：** 处理各种错误情况，给出友好提示

### 2. 客服管理

- **及时回复：** 设置客服工作时间，及时回复用户
- **消息模板：** 准备常用回复模板，提高效率
- **数据记录：** 记录咨询记录，便于后续跟进

### 3. 技术实现

- **渐进增强：** 先实现基础功能，再逐步优化
- **错误监控：** 添加错误日志，便于排查问题
- **性能优化：** 客服消息推送注意频率限制

---

## 🚨 注意事项

1. **主体限制：** 个人主体小程序不支持客服功能
2. **认证要求：** 需要小程序认证（企业主体）
3. **客服配置：** 必须在微信后台配置客服人员
4. **消息限制：** 客服消息有48小时有效期
5. **频率限制：** 注意API调用频率限制
6. **HTTPS要求：** 如果使用消息推送，需要HTTPS服务器

---

## 📚 参考文档

- [微信小程序客服消息API](https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/customer-message/customerServiceMessage.send.html)
- [小程序客服会话](https://developers.weixin.qq.com/miniprogram/dev/api/custom-message/wx.openCustomerServiceConversation.html)
- [微信公众平台客服功能](https://kf.qq.com/)

---

## 🎯 总结

**推荐实现方案：**

1. **第一阶段（必须）：** 小程序前端实现客服按钮和会话打开功能
   - 工作量：3-5小时
   - 复杂度：⭐⭐☆☆☆

2. **第二阶段（推荐）：** 优化用户体验，添加房源信息格式化
   - 工作量：2-3小时
   - 复杂度：⭐⭐☆☆☆

3. **第三阶段（可选）：** 实现后端客服消息推送
   - 工作量：6-8小时
   - 复杂度：⭐⭐⭐⭐☆

**总体评估：**
- **最小实现：** 3-5小时，简单
- **完整实现：** 10-15小时，中等偏复杂
- **推荐方案：** 先实现第一阶段，根据实际需求决定是否继续

