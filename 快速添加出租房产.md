# 快速添加出租房产数据

## 问题诊断

当前问题：
1. ✅ API工作正常
2. ✅ 小程序默认显示出租房产
3. ❌ 数据库中没有出租房产数据（只有28个出售房产）

## 解决方案

### 方案1：爬取出租房产并导入数据库

#### 步骤1：爬取出租房产数据
```bash
python scrape_rental.py
```

这将：
- 从Zoopla爬取出租房产数据
- 保存到 `rental_properties.csv`
- 自动设置 `listing_type` 为 `for_rent`

#### 步骤2：导入到数据库（追加模式）
```bash
python import_to_db.py --file rental_properties.csv
```

这将：
- 追加出租房产数据到数据库
- 保留现有的28个出售房产

### 方案2：手动修改现有数据

如果想将部分现有出售房产改为出租房产：

```python
import pymysql
from backend.config import Config

conn = pymysql.connect(
    host=Config.DB_HOST,
    port=Config.DB_PORT,
    user=Config.DB_USER,
    password=Config.DB_PASSWORD,
    database=Config.DB_NAME,
    charset='utf8mb4'
)
cursor = conn.cursor()

# 将前10个房产改为出租
cursor.execute("""
    UPDATE properties 
    SET listing_type = 'for_rent' 
    WHERE id <= 10
""")
conn.commit()

cursor.close()
conn.close()
```

## 验证

### 1. 检查数据库数据
```bash
python test_listing_type.py
```

应该看到两种类型的房产数量。

### 2. 测试API
```bash
# 获取出租房产（应该返回数据）
curl "http://localhost:5000/api/properties?listing_type=for_rent"

# 获取出售房产（应该返回数据）
curl "http://localhost:5000/api/properties?listing_type=for_sale"

# 获取所有房产（应该返回所有数据）
curl "http://localhost:5000/api/properties"
```

### 3. 测试小程序
1. 打开小程序
2. 默认显示"出租"房产
3. 点击"出售"标签，显示出售房产
4. 点击"出租"标签，显示出租房产

## 常用命令

### 只爬取出租房产
```bash
python scrape_rental.py
```

### 只爬取出售房产
```bash
python Scrapper.py --url "https://www.zoopla.co.uk/for-sale/property/n10/?q=N10&radius=1&search_source=for-sale"
```

### 清空并重新导入所有数据
```bash
# 1. 爬取出售房产
python Scrapper.py --output properties.csv

# 2. 爬取出租房产
python scrape_rental.py

# 3. 清空数据库并导入出售房产
python import_to_db.py --file properties.csv --clear

# 4. 导入出租房产（追加）
python import_to_db.py --file rental_properties.csv
```

### 查看数据库状态
```bash
python test_listing_type.py
```

## 注意事项

1. **不要使用 `--clear` 两次**：否则会删除之前导入的数据
2. **推荐先爬取出租房产**：因为小程序默认显示出租房产
3. **URL可能过期**：如果爬取失败，需要更新URL
4. **遵守爬虫规则**：不要过于频繁爬取，遵守robots.txt

## 故障排除

### 问题：爬取返回空数据

**原因**：URL可能失效或网站结构改变

**解决**：
1. 手动访问URL，确认页面正常
2. 更新URL中的区域代码（n10改为其他区域）
3. 检查网络连接

### 问题：导入时出错

**解决**：
```bash
# 检查CSV文件是否正确
head -5 rental_properties.csv

# 手动查看错误信息
python -c "
import csv
with open('rental_properties.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for i, row in enumerate(reader):
        if i < 3:
            print(row)
"
```

### 问题：API仍然返回旧数据

**解决**：
```bash
# 重启后端服务器
# 停止当前运行的服务器（Ctrl+C）
# 重新启动
cd backend
python app.py
```

## 下一步

完成上述步骤后：
1. ✅ 测试API返回正确的出租房产
2. ✅ 测试小程序显示出租房产
3. ✅ 切换"出售/出租"标签正常工作
4. ✅ 所有筛选功能正常

