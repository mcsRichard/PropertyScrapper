# 🚀 启动GUI工具 - 快速指南

## 一键启动

```bash
python property_gui.py
```

或使用启动脚本（自动检查依赖）：

```bash
python run_gui.py
```

---

## 📋 功能一览

### ✅ 已实现功能

1. **图片存储**
   - 批量下载所有房产图片
   - 本地保存到 `property_images/` 目录
   - 智能去重，不重复下载

2. **GUI显示**
   - 图形界面浏览房产数据
   - 实时搜索过滤
   - 图片预览和详情查看

3. **数据浏览**
   - 列表导航
   - 上一个/下一个
   - 详细信息显示

---

## 🎯 使用步骤

### 第一步：安装依赖

```bash
pip install Pillow requests
```

### 第二步：启动GUI

```bash
python property_gui.py
```

### 第三步：下载图片

点击界面上的 **"下载所有图片"** 按钮

### 第四步：浏览房产

- 在左侧列表中选择房产
- 或使用底部的"上一个"/"下一个"按钮

---

## 🖼️ 界面说明

```
┌──────────────────────────────────────────┐
│  [加载CSV]  [下载所有图片]  [清除缓存] │
├─────────────┬───────────────────────────┤
│  搜索框     │                            │
│             │    房产图片显示区          │
│  ┌────────┐ │     (600x400)             │
│  │ 房产列表│ │                            │
│  │        │ │                            │
│  └────────┘ │                            │
│             ├───────────────────────────┤
│             │                            │
│             │  房产详细信息              │
│             │  ┌────────────────────────┐ │
│             │  │ 标题: xxx             │ │
│             │  │ 价格: £xxx           │ │
│             │  │ 描述: xxx...          │ │
│             │  └────────────────────────┘ │
├─────────────┴───────────────────────────┤
│  [上一个] [下一个]    第 3 / 28          │
└──────────────────────────────────────────┘
```

---

## 📁 文件说明

### 核心文件
- `property_gui.py` - GUI主程序（约350行）
- `run_gui.py` - 启动脚本
- `properties.csv` - 数据文件

### 文档
- `GUI_README.md` - 完整使用说明
- `GUI_QUICKSTART.md` - 快速开始指南
- `GUI_FEATURES.md` - 功能详细说明

### 自动生成
- `property_images/` - 图片存储目录

---

## ⚡ 快速操作

| 操作 | 方法 |
|------|------|
| 加载数据 | 点击"加载CSV"或自动加载 |
| 下载图片 | 点击"下载所有图片" |
| 查看房产 | 点击列表中的房产 |
| 搜索房产 | 在搜索框输入关键词 |
| 导航 | 点击"上一个"/"下一个" |
| 清除缓存 | 点击"清除图片缓存" |

---

## 🔧 故障排除

### 问题1: 无法启动GUI

**解决方案**:
```bash
# Windows
pip install Pillow requests

# Linux
sudo apt-get install python3-tk
pip3 install Pillow requests

# macOS
pip3 install Pillow requests
```

### 问题2: 图片显示不出来

**解决方案**:
1. 点击"下载所有图片"
2. 等待下载完成
3. 图片会自动显示

### 问题3: 下载很慢

**原因**: 需要从网络下载图片  
**解决方案**: 耐心等待或使用VPN加速

---

## 📊 数据统计

当前数据：
- 房产数量: 28条
- 图片数量: 28张
- 数据大小: ~10KB CSV
- 图片大小: ~50-500KB/张

---

## 🎨 功能演示

### 功能1: 图片存储

```python
# 自动化下载所有图片
点击按钮 → 下载线程 → 保存到本地 → 缓存到内存 → 显示在GUI
```

### 功能2: 图片显示

```python
# 智能加载和缓存
检查缓存 → 加载本地 → 下载网络 → 调整大小 → 显示
```

---

## 💡 使用技巧

1. **快速搜索**: 输入房产关键词快速定位
2. **批量下载**: 首次使用建议下载所有图片
3. **清除缓存**: 定期清理不需要的图片
4. **窗口大小**: 可以调整窗口大小查看更清晰的图片

---

## 🎯 下一步

现在你可以：
- ✅ 浏览所有房产数据
- ✅ 查看房产图片
- ✅ 搜索特定房产
- ✅ 管理本地图片

**开始使用**：
```bash
python property_gui.py
```

祝使用愉快！🎉



