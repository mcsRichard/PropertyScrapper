# 为什么选择Web函数？

## 🎯 答案：选择 Web函数

---

## 📊 对比

### Web函数 ✅（推荐）

**优点**：
- ✅ **直接支持Flask**，无需修改代码
- ✅ 自动处理HTTP请求
- ✅ 上传Flask应用后直接运行
- ✅ 更接近本地开发体验

**使用方式**：
```python
# 只需要你现有的 app.py
from app import create_app
app = create_app()
```

**就是这么简单！** 🎉

---

### 事件函数 ❌（不推荐）

**缺点**：
- ❌ 需要写包装函数处理事件
- ❌ 需要自己解析HTTP请求
- ❌ 需要额外转换代码
- ❌ 配置复杂

**使用方式**：
```python
# 需要额外写包装函数
from app import create_app
app = create_app()

def main_handler(event, context):
    # 需要手动处理HTTP请求
    # 需要解析 event 参数
    # 需要返回特定格式
    # ... 复杂的转换代码
```

---

## 💡 为什么选择Web函数

### 1. 你已经有完整的Flask代码

你的项目结构：
```
backend/
├── app.py           ← Flask应用
├── config.py
├── api/
│   └── properties.py
├── models/
└── utils/
```

这些代码都是标准的Flask应用，Web函数可以直接运行！

---

### 2. 无需修改代码

选择Web函数后：
- ✅ 直接上传 `backend/` 目录
- ✅ 入口文件设置为 `app.py`
- ✅ 自动运行，无需额外配置

选择事件函数后：
- ❌ 需要写包装函数
- ❌ 需要处理event参数
- ❌ 需要返回特定格式
- ❌ 修改代码复杂

---

### 3. 官方推荐

腾讯云官方文档明确说明：
> "Web函数支持直接运行Flask、Django等Web框架"

---

## ✅ 结论

**选择：Web函数** ✅

**原因**：
1. 直接支持你现有的Flask代码
2. 无需额外修改
3. 配置简单
4. 官方推荐

---

## 🚀 创建云函数时的选择

```
请选择函数类型：
┌─────────────────────────────────┐
│ ○ 事件函数                      │
│ ● Web函数        ← 选这个！     │
└─────────────────────────────────┘
```

---

## 📝 创建后的配置

选择Web函数后，还需要设置：

### 1. 入口函数

```
入口函数：app.app
  （指向你的Flask应用对象）
  
或者：

入口文件：app.py
入口函数：app
```

### 2. 触发配置

选择Web函数后，会自动配置HTTP触发器：
- 路径：`/*`
- 方法：GET、POST、PUT、DELETE等
- 自动处理CORS

---

## 🎯 完整配置示例

### 创建时：

```
函数名称：property-api
地域：华东（上海）
函数类型：Web函数        ← 选择这个
运行环境：Python 3.7
```

### 部署时：

```
入口文件：app.py
入口函数：app

或者直接在代码中：
from app import app
```

### 代码结构：

```
你的代码：
backend/
├── app.py               ← Flask应用入口
├── requirements.txt
└── ...

上传时：
云函数会直接运行 app.py 中的 Flask 应用
```

---

## ❓ 常见问题

### Q1: Web函数和事件函数有什么区别？

**答**：
- **Web函数**：专门为HTTP请求设计，直接支持Flask、Django
- **事件函数**：通用函数，需要自己处理所有逻辑

### Q2: 我的代码能直接使用吗？

**答**：
- ✅ 能！直接上传你的Flask代码
- ✅ Web函数会自动处理HTTP请求
- ✅ 无需修改任何代码

### Q3: 如果选错了怎么办？

**答**：
- 删除重新创建（推荐）
- 或者写包装函数转换

### Q4: 会影响备案吗？

**答**：
- ✅ 不会！两种类型都可以备案
- ✅ 都在云函数SCF服务下
- ✅ 备案流程完全一样

---

## 🎉 总结

**记住**：
- ✅ 选择"Web函数"
- ✅ 上传你的Flask代码
- ✅ 自动运行，无需修改

**就这么简单！** 🚀

